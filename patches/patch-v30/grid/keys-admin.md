# Прогон по сетке: группа keys-admin

Виджеты категорий «Ключи» (`key_card`, `keys_by_company`, `keys_expiring`, `license_registry`,
`renewals`) и «Администрирование» (`auth_failed`, `consts_watch`, `system_health`, `users_online`).
Стили группы — `public/metronic/css/osmo-desktop-widgets/keys-admin.css`.

## Аудит 23.09.2026 — коротко

Сверка с источниками: «Реестр лицензий» `/analytics/licenses` (`LicenseRegistryService`),
`LicenseRenewalService`, админ-панель (`/admin/users`, `user_auth_attempts`, `/admin/consts`).
Горизонты — от 23.09.2026, граница включительно, как на странице (`days <= N`).

Неточности, которые исправлены:
- **Ссылки на реестр врали.** Страница без `horizon` в адресе берёт 90 дней и только активные ключи,
  поэтому `license_registry` («Все лицензии», 55 ключей) вёл на страницу с 14 строками, `keys_by_company`
  и `renewals` — туда же, `key_card` без номера ключа не находил ключ с далёким сроком. Теперь «все лицензии»
  передаются явно пустым `horizon=`; `key_card` ищет по номеру ключа, а без него — по названию компании
  (РРПК: 15 строк на странице = 15 ключей компании).
- **Партнёр ключа без спецификации.** Реестр берёт партнёра договора, а если его нет — партнёра компании
  (`COALESCE(c.partner_id, cm.partner_id)`); виджеты брали только договор. `keys_by_company` в разрезе
  «Партнёр» показывал «Без партнёра — 24» (у реестра таких нет, Softline 34 вместо 15), `renewals` при отборе
  по партнёру терял ключи без спецификации (SINAPSYSTEC: 0 → 1 продление за 12 мес.), `key_card` не показывал
  партнёра у НЛМК, Русала, Клубов Испании. Везде теперь как в реестре.
- `key_card`: пусто по-разному — «Ключ не выбран» (ничего в настройках) и «Ключ не найден» (подсказка:
  нет ключа с таким номером / у компании нет ключей среди активных); раньше на всё «не выбран». Цвет числа —
  по состоянию реестра (горизонты `license_horizons`), а не по зашитым 30/60. Строка без договора подписана
  «Партнёр», а не «Договор».
- `license_registry`, `renewals`: у ключа без спецификации в строке «—», как на странице, а не «0 ₽»
  (в реестре таких 17 из 55).
- `keys_expiring`: подсказка суммы говорит, из чего она сложена — при «Учитывать уже истёкшие» в неё входят
  и истёкшие за хвост (сейчас 1 362 000 ₽ = 1 истекающий + 2 истёкших; число сверху — только истекающие).
- `renewals`: превью библиотеки подписывало период «Квартал» — теперь период и валюта из контекста, как в `data()`.
- `consts_watch`: пустое состояние «Константы не выбраны» было неверным (ничего не выбрано — это все
  несистемные) — теперь «Констант нет» / «Выбранные константы не найдены».
- `system_health`: в образце даты были зашиты (14.09.2026) и подписи расходились с `data()` — теперь от сегодня
  и те же слова.
- Описания `keys_by_company` («Топ компаний… активных» — а разрез бывает партнёр/страна, активность — настройка)
  и `license_registry` («Действующие лицензии» — а по умолчанию там и истёкшие) приведены к поведению.

Вёрстка (многоточия):
- Таблицы с несколькими `.desk-cut`: у `.desk-cut` ширина 99%, и первая такая колонка забирала всё место —
  в `license_registry` компания и спецификация («ООО «Альфа»») резались на 16 колонках, в `users_online`
  должность «Менеджер», в `consts_watch` значение `uf_crm_1719404976291` и короткое примечание. Колонкам заданы
  доли в стилях группы.
- `key_card`: от 300 px высоты спецификация, номер ключа (`softline_rrpk_ms_…`, 50 знаков) и договор
  переносятся, а не режутся — T 31 → 18.
- `system_health`: название подсистемы в плитке переносится по словам; «Синхронизация Битрикс24» → «Битрикс24»
  (слово «синхронизация» ушло в пояснение), «записи за последние сутки есть» → «есть записи за сутки» — T 100 → 20.
- `renewals`: подпись «продлено · Текущий квартал» — период только шире 230 px (уже он резался).

На решение владельца (не правилось — общий сервис и страница):
- Сумма ключа в реестре — полная сумма спецификации на каждый ключ (`LicenseRegistryService::decorate()`), а
  «Истекающие ключи» и «Продления» делят её между ключами спецификации (`LicenseRenewalService::renewalAmount()`).
  Спецификаций с несколькими ключами 6 (у одной 6 ключей): итог реестра 47 994 720 ₽, с делением — 37 878 711 ₽.
  `license_registry` повторяет страницу.
- Реестр не знает «хвоста» истёкших (`license_expired_tail_days`): по ссылке из «Истекающих ключей» на странице
  все 9 истёкших за всё время, в виджете — 2 за 30 дней.
- Страница реестра с горизонтом не из `license_horizons` (в «Истекающих ключах» можно задать 45 дней) фильтрует
  по 45, но в списке горизонтов опции нет — браузер показывает первую, «только истекшие».

Доступ: все четыре админ-виджета — `available()` = `isPanelAdmin()` (то же, что Gate `admin_panel`);
проверено: пользователь 5 (не админ) — недоступны, пользователь 1 — доступны; стол, библиотека, настройки
и перерисовка блока проверяют `WidgetRegistry::availableFor()` для смотрящего.

## key_card — Карточка ключа

Правки: вьюха переложена в `.desk-stack` (компания сверху, число с подписью посередине,
подробности прижаты книзу); стили `.kc-*` в файле группы. Класс и `sample()` не менялись.

Поведение по размерам:
- ширина 2 и высота ≤4: только остаток дней (компания и срок — в `title` числа);
- высота 2 и ширина ≤6: компания + число; ширина ≥8 при высоте 2: рядом с числом «дн. до дата» и бейдж состояния;
- ширина 2: даты короткие (`05.10.26`, `fs-8`), подпись «дн. до» и дата переносятся строками;
- высота ≥4: + спецификация (`desk-nowrap` с `title`), отметка «ключ не активен»;
- высота ≥6: + номер ключа и договор/партнёр (кроме узкого блока), срок «с — по» и полоска прошедшей доли срока цвета состояния;
- высота ≥6 и ширина ≥4: число крупнее (`desk-value-lg`);
- высота от 300 px (≥8 ячеек): спецификация, номер ключа и договор переносятся строками (`.kc-wrap`).

Аудит 23.09: партнёр ключа без спецификации — партнёр компании (как в реестре); `chosen` в данных и образце —
«Ключ не выбран» / «Ключ не найден»; цвет числа — `state_color` реестра; строка без договора — «Партнёр»;
ссылка в шапке — реестр с `horizon=` (все лицензии), `q` = номер ключа или название компании, `only_active`
по настройке.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    Te    ·     ·     ·     ·     ·     ·     ·
4    T     T     ·     ·     ·     ·     ·     ·
6    T     T     ·     ·     ·     ·     ·     ·
8    T     ·     ·     ·     ·     ·     ·     ·
10   T     ·     ·     ·     ·     ·     ·     ·
12   T     ·     ·     ·     ·     ·     ·     ·
14   T     ·     ·     ·     ·     ·     ·     ·
16   T     ·     ·     ·     ·     ·     ·     ·
18   T     ·     ·     ·     ·     ·     ·     ·
20   T     ·     ·     ·     ·     ·     ·     ·
22   T     ·     ·     ·     ·     ·     ·     ·
24   T     ·     ·     ·     ·     ·     ·     ·
26   T     ·     ·     ·     ·     ·     ·     ·
28   T     ·     ·     ·     ·     ·     ·     ·
30   T     ·     ·     ·     ·     ·     ·     ·
32   T     ·     ·     ·     ·     ·     ·     ·
Итого: T многоточие: 18; e занято < 1/4: 1
```

Осталось:
- T в ширине 2 — заголовок виджета «Карточка ключа» в шапке (общий `partials/widget.blade.php`,
  у `.desk-widget-title` нет `title`), в ширине 2 и 4 при высоте до 6 — спецификация (`title` с договором);
- e на 2×2 — видно главное число, допустимо.
- Живые данные (компания РРПК, ключ `softline_rrpk_ms_…`, 20 дн. до 13.10.2026, «Меньше месяца» — совпадает
  с реестром): T только на 2×2 (заголовок) и 2×6/4×6 (номер ключа и договор — ниже 300 px они режутся);
  НЛМК без спецификации — «Партнёр Softline»,
  чисто; неверный номер — «Ключ не найден».

## keys_by_company — Ключи по компаниям

Правки: вьюха — `.desk-stack` с итогом сверху, список/таблица в `.desk-fit` на `$rows_max`
(было `$rows` в `.desk-scroll`), подпись «ещё N»; в классе `sample()` — 40 строк с отсечкой по
`limit` (как в `data()`), умолчание настройки «Сколько строк готовить» 15 → 40. `data()` не менялся.

Поведение по размерам:
- ширина 2–10 (dw xs|sm|md): список «название — число ключей» (`badge`);
- ширина ≥6 (шире 230 px): + ближайшее окончание; ширина ≥10 (шире 420 px): + доля в процентах;
- ширина ≥12 (dw lg|xl): таблица «компания — ключей — доля с полоской», ширина ≥14 (шире 620 px) — + ближайшее окончание;
- высота ≥6 (выше 230 px): сверху итог — всего ключей (`desk-value-sm`), число групп и полоска долей первых пяти (кроме узкого блока);
- строк с запасом `$rows_max`, не влезшие прячет `.desk-fit`; подпись «ещё N» (кроме низкого блока).

Аудит 23.09: разрез «Партнёр» — `COALESCE(партнёр договора, партнёр компании)`, как в реестре (живые:
Softline 34, LURE IT 10, SINAPSYSTEC 3…, «Без партнёра» больше нет; всего 55 = реестр «все, активные»);
ссылка в шапке — реестр с `horizon=`; описание — про разрез, а не только «топ компаний». Вёрстка не менялась.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    Tf    f     f     f     f     f     f     f
4    Tf    f     f     f     f     f     f     f
6    Tf    f     f     f     f     f     f     f
8    Tf    f     f     f     f     f     f     f
10   Tf    Tf    f     f     f     f     f     f
12   Tf    Tf    f     f     f     f     f     f
14   Tf    Tf    f     f     f     f     f     f
16   Tf    Tf    f     f     f     f     f     f
18   Tf    Tf    Tf    f     f     f     f     f
20   Tf    Tf    Tf    f     f     f     f     f
22   Tf    Tf    Tf    f     f     f     f     f
24   Tf    Tf    Tf    f     f     f     f     f
26   Tf    Tf    Tf    f     f     f     f     f
28   T     T     T     ·     ·     ·     ·     ·
30   T     T     T     ·     ·     ·     ·     ·
32   T     T     T     ·     ·     ·     ·     ·
Итого: T многоточие: 36; f подгон спрятал строки: 104
```

Осталось:
- T — названия компаний в строках списка (`desk-grow` с `title`) в узких блоках и заголовок виджета в ширине 2;
- f — подгон прятал строки: в образце 40 групп, это ожидаемо.
- Полоска доли в строке списка (`.desk-bar` фиксированной ширины) давала H на 10×18…32 — заменена процентом.
- Живые данные (15 компаний): только T на 2×2 и f; без заголовка — T на 2×2, f; `render_widgets.php` — 16 проверок, ошибок нет.

## keys_expiring — Истекающие ключи

Правки: вьюха — `.desk-stack`: подпись, строка «число + сумма/истёкшие» (переносится),
список ближайших в `.desk-fit` на `$rows_max` (было `$rows - 3` в `.desk-clip`); стили `.ke-*`.
В классе: `data()` отдаёт до 30 ближайших ключей вместо 3 (выборка та же, ключи уже в памяти) —
иначе высокий блок нечем заполнить; `sample()` — 30 ключей в пределах горизонта.

Поведение по размерам:
- высота 2, ширина ≤6: подпись и число; ширина ≥8 (шире 300 px): рядом «продление сумма» и «истекли: N»;
- ширина 2: подпись короче («за 30 дн.»), сумма без слова «продление», `fs-8`;
- высота ≥4: + сумма и истёкшие под/рядом с числом, список ближайших ключей «компания — N дн.» (`.desk-fit`, `data-fit-min="0"`, «ещё N»);
- ширина ≥6 (шире 230 px): в строке + дата окончания; «без курса: N» при пропусках;
- высота ≥10 и ширина ≥6: число крупнее (`desk-value-lg`).

Аудит 23.09 (живые, горизонт 30): 1 истекающий ключ (РРПК, 13.10.2026, 20 дн.) = карточка реестра
«Истекает ≤ 30 дн.» 1 / 506 000 ₽; «истекли: 2» — за хвост 30 дн. (22.09.2026); сумма 1 362 000 ₽ = 506 000 +
380 000 + 476 000 (с истёкшими) — подсказка суммы теперь это расписывает. Вёрстка не менялась.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    T     ·     ·     ·     ·     ·     ·     ·
4    Tf    f     f     f     f     f     f     f
6    Tf    f     f     f     f     f     f     f
8    Tf    f     f     f     f     f     f     f
10   Tf    f     f     f     f     f     f     f
12   Tf    Tf    f     f     f     f     f     f
14   Tf    Tf    Tf    f     f     f     f     f
16   Tf    Tf    Tf    f     f     f     f     f
18   Tf    Tf    Tf    f     f     f     f     f
20   Tf    Tf    Tf    f     f     f     f     f
22   T     T     T     ·     ·     f     f     f
24   T     T     T     ·     ·     ·     ·     ·
26   T     T     T     ·     ·     ·     ·     ·
28   T     T     T     ·     ·     ·     ·     ·
30   T     T     T     ·     ·     ·     ·     ·
32   T     T     T     ·     ·     ·     ·     ·
Итого: T многоточие: 37; f подгон спрятал строки: 75
```

Осталось:
- T — названия компаний в строках (`desk-grow` с `title`) и заголовок виджета в ширине 2;
- ссылка ведёт на реестр `horizon=30&only_active=1`: там 10 строк — 1 истекающий и 9 истёкших за всё время
  (у страницы нет «хвоста» `license_expired_tail_days`), а виджет считает истёкшие только за хвост;
- «ещё N» считает только подготовленные строки (до 30), а не все истекающие ключи — полное число и так крупно сверху.
- Живые данные (3 ключа): T на 2×2, f; e на 16×32 — ключей мало, это данные, а не вёрстка;
  без заголовка — чисто; `render_widgets.php` — 14 проверок, ошибок нет.

## license_registry — Реестр лицензий

Правки: вьюха — `.desk-stack`, таблица и список в `.desk-fit` на `$rows_max` (было `$rows` в
`.desk-scroll`), подпись «ещё N»; итог таблицы вынесен в `<tfoot>` без `desk-cut` (раньше
«Итого · 5 лицензий» уходило в многоточие вместе с числом); у списка — итог внизу; стили `.lr-*`.
В классе `sample()` — 40 лицензий с отсечкой по `limit`, итоги по всем. `data()` не менялся.

Поведение по размерам:
- ширина 2–10 или высота 2 (не таблица): список «компания — остаток дней (бейдж цвета состояния)»;
- ширина 2: остаток — только число со знаком («−167»), полностью — в `title`;
- шире 230 px: + дата окончания; шире 420 px: + сумма; шире 620 px: + партнёр;
- высота ≥6 у списка: внизу итог «N лицензий · сумма»;
- ширина ≥12 и высота ≥4: таблица «компания — осталось», шире 230 px — окончание, шире 420 px — спецификация и сумма,
  шире 620 px — партнёр; подвал «Итого — N компаний — N лицензий — сумма»;
- строки с запасом `$rows_max`, лишние прячет `.desk-fit`;
- в таблице у обрезаемых колонок доли ширины: шире 420 px компания 55 % / спецификация 45 %, шире 620 px
  партнёр 24 % / компания 40 % / спецификация 36 % (без спецификации — 30 / 70).

Аудит 23.09: ссылка — реестр с тем же горизонтом, «все лицензии» — `horizon=` (было: без горизонта → страница
на 90 днях, 14 строк против 55 в виджете); у ключа без спецификации сумма «—», как на странице (17 из 55);
описание — «как на странице реестра», без «действующие». Живые: 55 лицензий, 15 компаний, 47 994 720 ₽ —
совпадает со страницей «все, активные». Таблица: первая `.desk-cut` (99 %) забирала всё место — доли в стилях.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    Tf    Tf    f     f     f     f     f     f
4    Tf    Tf    f     f     f     Tf    Tf    Tf
6    Tf    Tf    Tf    f     f     Tf    Tf    Tf
8    Tf    Tf    Tf    f     f     Tf    Tf    Tf
10   Tf    Tf    Tf    f     f     Tf    Tf    Tf
12   Tf    Tf    Tf    Tf    Tf    Tf    Tf    Tf
14   Tf    Tf    Tf    Tf    Tf    Tf    Tf    Tf
16   Tf    Tf    Tf    Tf    Tf    Tf    Tf    Tf
18   Tf    Tf    Tf    Tf    Tf    Tf    Tf    Tf
20   Tf    Tf    Tf    Tf    Tf    Tf    Tf    Tf
22   T     T     T     T     T     T     T     T
24   T     T     T     T     T     T     T     T
26   T     T     T     T     T     T     T     T
28   T     T     T     T     T     T     T     T
30   T     T     T     T     T     T     T     T
32   T     T     T     T     T     T     T     T
Итого: T многоточие: 113; f подгон спрятал строки: 80
```

Осталось:
- T — только названия: компания в строке списка и таблицы (`title` с ключом), спецификация (`title` с договором),
  партнёр; в образце 40 названий с «(филиал N)», в высоком блоке всегда есть длинное — это реестр, допустимо;
  число T не упало, но на 16 колонках режутся только длинные («ООО «Магистраль-Логистик»»), а не «ООО «Альфа»»;
  заголовок виджета в ширине 2;
- f — лицензий больше, чем строк в блоке.
- Живые данные: C на 2×2 от бейджа «−167 дн.» — исправлено (только число); после правки T и f;
  `render_widgets.php` — 16 проверок, ошибок нет.

## auth_failed — Неудачные входы

Правки: вьюха — счётчик сверху (`.af-head`), попытки в `.desk-fit` на `$rows_max` (было
`$rows - 1` в `.desk-scroll`), «ещё N»; в строке списка логин больше не обёрнут вместе с IP
в один `desk-grow`; колонка «Браузер» без `desk-cut` (подписи короткие, сужается логин); стили
`.af-*`. В классе: `sample()` — до 40 попыток с отсечкой по `limit`, умолчание «Сколько попыток
в списке» 15 → 40 (максимум прежний, 50). `data()` не менялся.

Поведение по размерам:
- высота 2: подпись и счётчик; шире 300 px — рядом «порог N», «всплеск», «логинов/IP»;
- ширина 2: подпись «за сутки» без «не вошли», время попытки «ЧЧ:ММ» (сегодня) или «ДД.ММ»;
- высота ≥4: счётчик сверху, ниже последние попытки: список «логин — сколько назад», шире 420 px — + IP;
- ширина ≥12 и высота ≥4: таблица «логин — когда», шире 420 px — IP, шире 620 px — браузер;
- строки с запасом `$rows_max`, лишние прячет `.desk-fit`.

Аудит 23.09: без правок. Живые: за сутки 0 неудачных (последняя — 15.09.2026 00:38), список показывает
последние 40 попыток независимо от окна счётчика — так и задумано («последние попытки»); логины сопоставлены
с пользователями (avg.anna → карточка 1). Доступ — только панель-админу.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    T     ·     ·     ·     ·     ·     ·     ·
4    Tf    Tf    f     f     f     f     f     f
6    Tf    Tf    Tf    f     f     f     f     f
8    Tf    Tf    Tf    f     f     f     f     f
10   Tf    Tf    Tf    f     f     f     f     f
12   Tf    Tf    Tf    f     f     f     f     f
14   Tf    Tf    Tf    f     f     f     f     f
16   Tf    Tf    Tf    f     f     f     f     f
18   Tf    Tf    Tf    f     f     f     f     f
20   Tf    Tf    Tf    f     f     f     f     f
22   Tf    Tf    Tf    f     f     f     f     f
24   Tf    Tf    Tf    f     f     f     f     f
26   Tf    Tf    Tf    f     f     f     f     f
28   T     T     T     ·     ·     ·     ·     ·
30   T     T     T     ·     ·     ·     ·     ·
32   T     T     T     ·     ·     ·     ·     ·
Итого: T многоточие: 45; f подгон спрятал строки: 96
```

Осталось:
- T — логины в строках узкого списка (`title` с IP, временем и браузером) и заголовок виджета в ширине 2;
- s (прокрутка) было на ширине 2 и 6 — ушло вместе с `.desk-scroll`.
- Живые данные: T на 2×2 и 16×32 (длинный логин), f; без заголовка — чисто;
  `render_widgets.php` — 14 проверок, ошибок нет.

## consts_watch — Константы портала

Правки: вьюха — список и таблица в `.desk-fit` на `$rows_max` (было `$rows` в `.desk-scroll`),
«ещё N»; значение-число и короткое значение (до 16 знаков) выводятся без обрезки, длинное
(JSON, адрес) — многоточием с полным значением в `title`; примечание — отдельным элементом, а не
внутри ссылки названия; в узком блоке строка переносится (название сверху, значение ниже);
стили `.cw-*`. В классе `sample()` — 40 констант. `data()` не менялся.

Поведение по размерам:
- ширина 2 (уже 170 px): название строкой, под ним значение; целиком — только короткие числа;
- ширина 4–10: «название — значение» в строку, длинное значение не шире половины строки;
- шире 420 px: + примечание (если включено в настройках), не шире 30 %;
- ширина ≥12: таблица «константа — значение», шире 620 px — + примечание;
- в таблице с длинным значением (`cw-has-long`) название и значение делят место 50/50; с примечанием шире 620 px —
  60/40 или 38/34/28 (короткие значения не режутся и доли не получают);
- строки с запасом `$rows_max`, лишние прячет `.desk-fit`.

Аудит 23.09: пустое состояние — «Констант нет» / «Выбранные константы не найдены» (было «Константы не выбраны»,
хотя без выбора показываются все несистемные); таблица: первая `.desk-cut` (99 %) забирала всё место —
на 16×16 живые значения `uf_crm_1719404976291` резались, теперь целиком, режется только JSON. Живые: 30 констант
(31 минус 1 системная), ссылка строки — `/admin/consts?q={код}` (поиск по коду есть).

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    Tf    Tf    f     f     f     f     f     f
4    Tf    Tf    Tf    f     f     f     f     f
6    Tf    Tf    Tf    f     f     Tf    Tf    f
8    Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
10   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
12   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
14   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
16   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
18   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
20   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
22   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
24   Tf    Tf    Tf    Tf    Tf    Tf    Tf    f
26   Tf    T     T     T     T     T     T     ·
28   Tf    T     T     T     T     T     T     ·
30   Tf    T     T     T     T     T     T     ·
32   Tf    T     T     T     T     T     T     ·
Итого: T многоточие: 101; f подгон спрятал строки: 100
```

Осталось:
- T — названия констант и длинные строковые значения (адрес вебхука, почта, JSON) — с `title`;
  числа («22», «6000», «35») больше не режутся (в исходной матрице были в многоточии);
- f — констант больше, чем строк в блоке.
- Живые данные (коды полей Битрикс24 вида `uf_crm_1719404976291`, адреса): T на названиях и
  длинных значениях, f; без заголовка — T на 2×2 и 4×2; `render_widgets.php` — 16 проверок, ошибок нет.

## system_health — Состояние портала

Правки: вьюха — три режима по ступеням: светофор (тесный блок), список в `.desk-fit` на
`$rows_max` со счётчиком сверху (было `$rows` в `.desk-scroll`, пояснение вложено в `desk-grow`),
плитки подсистем на всю площадь для высоких блоков; стили `.sh-*` (сетка плиток 1/2/3 колонки
по ширине блока, значение переносится, а не режется, кегль значения — `clamp()` в `cqw`).
Класс и `sample()` не менялись: подсистем всего шесть, больше строк не бывает.

Поведение по размерам:
- ширина 2 и высота 2 (или высота в одну ячейку): «замечаний» — число — «из 6»;
- ширина ≥4 при высоте 2–8 и ширина ≤10 при высоте 6–8: список подсистем проблемами вверх —
  значок, название, значение; уже 170 px — цветная точка и название (значение в `title`),
  шире 420 px — + пояснение; выше 110 px — сверху счётчик замечаний и бейджи «ошибок / внимание»;
- высота ≥10 (любая ширина) и высота 6–8 при ширине ≥12: плитки на всю площадь — название,
  значение крупнее; не в узком блоке — + пояснение и время события;
  колонок: уже 230 px — 1, до 420 px — 2, шире — 3;
- название подсистемы в плитке шире 170 px переносится по словам (`.sh-tile-name`).

Аудит 23.09: «Синхронизация Битрикс24» → «Битрикс24», пояснение «синхронизация, таблиц: N»; «записи за
последние сутки есть» → «есть записи за сутки»; образец — даты от сегодня и те же подписи, что у `data()`
(было зашито 14.09.2026). Живые (23.09 14:16): очередь — 3 задачи ждут с 17.09.2024 (жёлтый), Битрикс24 —
3 дня назад, OSMOVIEW CP — 09.09.2026, журнал ошибок — записи за сутки есть, курсы — 23.09.2026, журнал
изменений — 169 за сутки: 4 «внимание», 0 ошибок. Значок курсов `fa-coins` (замена 23.09) сохранён.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    T     Tf    f     f     f     f     f     f
4    Tf    Tf    f     f     f     f     f     f
6    T     T     ·     ·     ·     ·     ·     ·
8    T     T     ·     ·     ·     ·     ·     ·
10   T     ·     ·     ·     ·     ·     ·     ·
12   T     ·     ·     ·     ·     ·     ·     ·
14   T     ·     ·     ·     ·     ·     ·     ·
16   T     ·     ·     ·     ·     ·     ·     ·
18   T     ·     ·     ·     ·     ·     ·     ·
20   T     ·     ·     ·     ·     ·     ·     ·
22   T     ·     ·     ·     ·     ·     ·     ·
24   T     ·     ·     ·     ·     ·     ·     ·
26   T     ·     ·     ·     ·     ·     ·     ·
28   T     ·     ·     ·     ·     ·     ·     ·
30   T     ·     ·     ·     ·     ·     ·     ·
32   T     ·     ·     ·     ·     ·     ·     ·
Итого: T многоточие: 20; f подгон спрятал строки: 15
```

Осталось:
- T — в ширине 2 (плитки в одну колонку уже 170 px, слово шире плитки) и в списке ширины 4 при высоте до 8
  (название сжимается ради значения «2 дня назад»); у строки и плитки полный `title` с значением, пояснением
  и временем; заголовок виджета в ширине 2;
- e в исходной матрице было на всех высоких блоках (77) — ушло с плитками; C на 2×10 (плитка
  в одну колонку) убрано плотными отступами в узком блоке.
- Живые данные (1 ошибка, 3 предупреждения): T на 4×2, f; 16×16 и 16×32 — чисто;
  без заголовка — T на 4×2, f; `render_widgets.php` — 16 проверок, ошибок нет.

## users_online — Пользователи: активность

Правки: вьюха — счётчик сверху (`.uo-head`, раньше одна строка «в сети 2 из 5» без переноса
обрезалась в ширине 2), пользователи в `.desk-fit` на `$rows_max` (было `$rows - 1` в
`.desk-scroll`), «ещё N»; имя и должность — отдельные элементы строки, а не спаны внутри одной
ссылки; в узком блоке время коротко; стили `.uo-*`. В классе: `sample()` — до 40 пользователей
с отсечкой по `limit`, умолчание «Сколько пользователей» 15 → 40 (максимум прежний, 50).
`data()` не менялся.

Поведение по размерам:
- высота 2: «в сети» и число; шире 300 px — рядом «из N» и бейдж неудачных входов за сутки;
- высота ≥4: счётчик «N в сети из M» сверху, ниже список «точка статуса — имя — когда был»;
- ширина 2: «из M» без «в сети», время — «сейчас», «ЧЧ:ММ» (сегодня) или «ДД.ММ»;
- шире 420 px: в строке списка + должность (не шире 35 %);
- ширина ≥12 и высота ≥4: таблица «пользователь — был», шире 420 px — последний вход, шире 620 px — должность;
- в таблице шире 620 px имя и должность делят место 55/45 %;
- строки с запасом `$rows_max`, лишние прячет `.desk-fit`.

Аудит 23.09: данные без правок — «в сети» то же правило, что `User::is_online` (600 с), активных 4 = таблица
админ-панели с отбором «активные», последний вход — `user_auth_attempts.success = 1`. Таблица: первая `.desk-cut`
(99 %) забирала всё место, и должность «Менеджер» уходила в многоточие на 14–16 колонках — доли в стилях.

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    T     T     ·     ·     ·     ·     ·     ·
4    Tf    Tf    f     f     Tf    f     f     f
6    Tf    Tf    Tf    f     Tf    f     Tf    f
8    Tf    Tf    Tf    f     Tf    f     Tf    Tf
10   Tf    Tf    Tf    f     Tf    f     Tf    Tf
12   Tf    Tf    Tf    f     Tf    f     Tf    Tf
14   Tf    Tf    Tf    f     Tf    f     Tf    Tf
16   Tf    Tf    Tf    f     Tf    f     Tf    Tf
18   Tf    Tf    Tf    f     Tf    f     Tf    Tf
20   Tf    Tf    Tf    f     Tf    f     Tf    Tf
22   Tf    Tf    Tf    f     Tf    f     Tf    Tf
24   Tf    Tf    Tf    f     Tf    f     Tf    Tf
26   Tf    Tf    Tf    f     Tf    f     Tf    Tf
28   T     T     T     ·     T     ·     T     T
30   T     T     T     ·     T     ·     T     T
32   T     T     T     ·     T     ·     T     T
Итого: T многоточие: 88; f подгон спрятал строки: 96
```

Осталось:
- T — имена в узком списке, должности (ширина 10 в списке, 14–16 в таблице — только длинные вроде
  «Руководитель направления внедрения») — второстепенный текст с `title`;
  заголовок «Пользователи: активность» в ширине 2 и 4 (шапка общая, `title` у неё нет);
- исходные C (15), s (30) и e (69) ушли.
- Живые данные (4 активных пользователя): T на 2×2 и 4×2 (заголовок), e на 16×32 — пользователей
  четыре, это данные; без заголовка — чисто; `render_widgets.php` — 16 проверок, ошибок нет.

## Итог по группе

После аудита 23.09 (образец, полная сетка 2…16 × 2…32; в скобках — прогон «до» того же дня):

| Виджет | X | C | H | F | G | T | e | s | f |
|---|---|---|---|---|---|---|---|---|---|
| key_card | 0 | 0 | 0 | 0 | 0 | 18 (31) | 1 | 0 | 0 |
| keys_by_company | 0 | 0 | 0 | 0 | 0 | 36 | 0 | 0 | 104 |
| keys_expiring | 0 | 0 | 0 | 0 | 0 | 37 | 0 | 0 | 75 |
| license_registry | 0 | 0 | 0 | 0 | 0 | 113 | 0 | 0 | 80 |
| renewals | 0 | 0 | 0 | 0 | 0 | 49 | 0 | 0 | 62 |
| auth_failed | 0 | 0 | 0 | 0 | 0 | 45 | 0 | 0 | 96 |
| consts_watch | 0 | 0 | 0 | 0 | 0 | 101 (112) | 0 | 0 | 100 |
| system_health | 0 | 0 | 0 | 0 | 0 | 20 (100) | 0 | 0 | 15 |
| users_online | 0 | 0 | 0 | 0 | 0 | 88 (91) | 0 | 0 | 96 |

T везде — только второстепенный текст с `title` (названия компаний, логины, имена, должности, спецификации,
длинные значения констант) и заголовок виджета в ширине 2; числа и суммы не режутся. Живые данные
(`--live`, 2×2 … 32×12) — без X/C/H/F/G; e на 16×16 у `keys_expiring` — истекает один ключ (данные).
`render_widgets.php` по группе — 136 проверок, ошибок нет.

### renewals — Продления

Исходная матрица: C 38, T 30, e 60 — на высоте 2 обрезались подпись и строка «новых ключей»,
на ширине 4 — «+29 % к прошлому отрезку» одним куском, высокие блоки пустовали (в `sample()` 3 строки).

Правки: вьюха — `.desk-stack`: подпись, строка «число + сумма, дельта, новые» (элементы переносятся
по одному, пояснение дельты отдельно), список продлений в `.desk-fit` на `$rows_max` (было
`$rows - 3` в `.desk-clip`); стили `.rn-*`. В классе: `totals()` собирает до 40 строк продлений
вместо 20 (ключи уже загружены, запрос тот же); `sample()` — 24 продления за текущий квартал.

Поведение по размерам:
- высота 2, ширина ≤6: подпись и число; ширина ≥8 (шире 300 px): рядом «на сумма» и дельта «+30 %»;
- ширина 2: подпись «продлено», сумма без «на» (`fs-8`), дельта без пояснения;
- высота ≥4: + «новых: N», «без курса: N»; шире 230 px — у дельты «к прошлому отрезку»;
- высота ≥4: список продлений «компания — сумма» (`.desk-fit`, `data-fit-min="0"`, «ещё N»), шире 230 px — + дата;
- высота ≥10 и ширина ≥6: число крупнее (`desk-value-lg`);
- подпись «продлено · Текущий квартал» — период только шире 230 px (`desk-only-w-md`), уже — «продлено».

Аудит 23.09: отбор по партнёру — как в реестре (партнёр договора, иначе партнёр компании): за 12 мес. по партнёрам
8 + 2 + 1 = 11 = все продления (SINAPSYSTEC было 0 — ключ Клубов Испании без спецификации); у ключа без
спецификации в строке «—», а не «0 ₽»; ссылка — реестр с `horizon=` (все лицензии) и партнёром; образец —
период и валюта из контекста («Текущий квартал 01.07.2026 – 30.09.2026», было «Квартал»). Живые: за текущий
квартал продлений нет, за 12 мес. — 11 на 8,7 млн ₽, новых 4, к прошлому отрезку −65 % (31).

Финальная матрица (образец, 23.09):

```
h\w  2     4     6     8     10    12    14    16
2    T     ·     ·     ·     ·     ·     ·     ·
4    Tf    Tf    f     f     f     f     f     f
6    Tf    Tf    f     f     f     f     f     f
8    Tf    Tf    f     f     f     f     f     f
10   Tf    Tf    f     f     f     f     f     f
12   Tf    Tf    Tf    f     f     f     f     f
14   Tf    Tf    Tf    f     f     f     f     f
16   Tf    Tf    Tf    f     f     f     f     f
18   T     T     Tf    f     f     f     f     f
20   T     T     T     T     ·     ·     ·     ·
22   T     T     T     T     ·     ·     ·     ·
24   T     T     T     T     ·     ·     ·     ·
26   T     T     T     T     ·     ·     ·     ·
28   T     T     T     T     ·     ·     ·     ·
30   T     T     T     T     ·     ·     ·     ·
32   T     T     T     T     ·     ·     ·     ·
Итого: T многоточие: 49; f подгон спрятал строки: 62
```

Осталось:
- T — названия компаний в строках (`title` с датой) и заголовок виджета в ширине 2; в ширине 2
  у части строк название сжимается до нуля (сумма важнее), полное — в `title`;
- промежуточный F (9,7 px на ширине 2: `.desk-delta` в `em` внутри `fs-8`) исправлен — `fs-8` только у суммы.
- Живые данные (за текущий квартал продлений нет): T на 2×2, остальное чисто — «За текущий квартал
  продлений не было»; без заголовка — f на 8×4; `render_widgets.php` — 14 проверок, ошибок нет.

Общие правки (не делались, файлы общие):
- `partials/widget.blade.php`: у `.desk-widget-title` нет `title` — многоточие заголовка в блоках
  ширины 2–4 даёт T во всех виджетах и полное название не прочитать.
- `osmo-desktop.css`: `.desk-table .desk-cut { width: 99% }` — при двух-трёх таких колонках первая забирает
  всё место (проценты сверх 100 браузер отдаёт по порядку колонок), остальные сжимаются до многоточия. В группе
  обойдено долями в `keys-admin.css`; то же, вероятно, у других виджетов с несколькими `.desk-cut` в таблице.
- Повторяющийся приём «число + пояснение рядом, пояснение прячется в низком нешироком блоке»
  (`.kc-main`, `.ke-main`, `.af-main`, `.uo-main`, `.sh-main`) стоит сделать общим классом тела
  в `osmo-desktop.css`, например `.desk-inline` + `.desk-inline-side`.

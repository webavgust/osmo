# Прогон по сетке: группа analytics

Виджеты: `china`, `report_download`, `scenarios_specs`, `scenarios_top`, `specs_summary`.
Стили группы — `public/metronic/css/osmo-desktop-widgets/analytics.css`.

## Китай: сводка — `china`

Правки: вьюха переписана, стили в `analytics.css`, `sample()` даёт 40 компаний (итоги
считаются по строкам). `data()` не менялся.

### Поведение по размерам
- ширина 2: плитки столбиком — ключей, сумма (подпись «сумма, ₽», число коротко), лицензий,
  компаний, стран, истекают; сколько влезло по высоте — решает `.desk-fit`. На 2×2 — только
  «ключей». Кнопка — один значок (высота ≥4).
- ширина ≥4: плитки в ряд — ключей и сумма КП (сумма вдвое шире); ≥230 px + лицензий,
  ≥420 px + компаний, ≥620 px + стран. Кегль чисел — от ширины с поправкой на число плиток.
- высота ≥3 (блок выше 110 px): строка пометок — истекают, партнёров (≥230 px),
  без курса (≥420 px).
- высота ≥4 (блок выше 150 px): кнопка «Сформировать отчёт» (узко — «Отчёт»).
- высота ≥6: таблица разреза (компании / страны / партнёры) до `$rows_max` строк в
  `.desk-fit` с подписью «и ещё N»; колонки: сумма всегда, лицензий ≥230 px, ключей ≥420 px,
  доля полоской с процентом ≥620 px.

### Матрица (образец)
```
china — Китай: сводка (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    Tf    ·     ·     ·     ·     ·     ·     ·     
4    Tf    ·     ·     ·     ·     ·     ·     ·     
6    Tf    Tf    Tf    f     f     f     f     f     
8    T     Tf    Tf    f     f     f     f     f     
10   T     Tf    Tf    f     f     f     f     f     
12   T     Tf    Tf    f     f     f     f     f     
14   T     Tf    Tf    Tf    f     f     f     f     
16   T     Tf    Tf    Tf    f     f     f     f     
18   T     Tf    Tf    Tf    f     f     f     f     
20   T     Tf    Tf    Tf    f     f     f     f     
22   T     Tf    Tf    Tf    f     f     f     f     
24   T     Tf    Tf    Tf    f     f     f     f     
26   T     Tf    Tf    Tf    f     f     f     f     
28   T     T     T     T     ·     ·     ·     ·     
30   T     T     T     T     ·     ·     ·     ·     
32   T     T     T     T     ·     ·     ·     ·     
Итого: T многоточие: 54; f подгон спрятал строки: 80
```
Живые данные (2x2, 4x2, 8x8, 16x16, 16x32): только T на 2×2 и f. Без заголовка (2x2, 4x2,
8x4): чисто, на 2×2 влезают ключи и сумма. `render_widgets.php china` — 14 проверок, 0 ошибок.

### Аудит 23.09.2026
- **Неточность: сумма КП считалась по ключам.** Отчёт повторяет сумму КП договора в строке
  каждого ключа этого договора, а виджет складывал строки. Договор P-АА-26/10.2024 (КП
  1 751 040 ₽) выдан восемью ключами (6 у «Русала», 2 у «ВВСС (Пулково)») и попадал в итог
  восемь раз. Было 28,9 млн ₽, стало 16,6 млн ₽ (BMW 11,28 млн + Метрополитен 3,58 млн +
  Русал 1,75 млн). Теперь сумма идёт один раз на договор (ключ → спецификация → договор), а в
  разрезе — в строку, где у договора больше ключей. Итог равен сумме строк во всех трёх
  разрезах. «Без курса» теперь считает договоры.
- Сверка с `/report/china` на живых данных совпала: 48 ключей (строки-ключи отчёта),
  623 лицензии (сумма колонки PLATFORM = `count` ключа), 5 истекающих (подсветка `warning`),
  12 компаний, 3 страны, 8 партнёров.
- Если у строки таблицы нет суммы КП, стоит «—» (в отчёте «-»), а не «0 ₽»; причина — в `title`.
- Кнопка называлась «Сформировать отчёт», теперь «Открыть отчёт»: она только открывает
  страницу, файл собирают там. Так же названа и настройка.
- Второстепенные КП (v33) на виджет не влияют: сумма берётся из КП самого договора, а не из
  списка КП.
- Живые данные (2x2, 4x2, 8x4, 8x8, 16x8, 32x8, 16x16, 6x10, 16x32): T только на заголовке
  при 2×2 и на названиях компаний при 6×10, иначе f.

### Что осталось
- T в колонке ширины 2 — заголовок виджета «Китай: сводка» в шапке (общий
  `partials/widget.blade.php`, в блок шириной 2 не влезает); без заголовка кода нет.
- T на ширине 4–8 — названия компаний в таблице (`.desk-cut` с `title`), числа и суммы
  целиком.
- Решает владелец: в «ключей» входят 2 выключенных ключа (`active = 0`: РРПК на 53 лицензии,
  Русал на 40). Отчёт показывает их вместе с остальными, с отметкой: класс `unactive` стоит
  на ячейках, а стиль в `osmo-compat-pages.css` написан только для `tr.unactive`, поэтому
  внешне их не отличить. В файл они тоже попадают; виджет считает так же.

## Выгрузка отчёта — `report_download`

Правки: вьюха переписана (плитки вместо ряда кнопок с `$max` по ступени), стили в
`analytics.css`, `sample()` даёт все 4 выгрузки (одна без даты). `data()` не менялся.

### Поведение по размерам
- Все выбранные выгрузки (до 4) — плитки-кнопки в сетке: колонок столько, сколько влезает
  по 150 px, строки делят высоту поровну; не влезшие плитки прячет `.desk-fit`.
  Итого: 4×2 и 4×4 — 1–2 плитки, 8×2 — две в ряд, 16×2 — четыре в ряд, 4×8 — четыре столбиком,
  8×8 — 2×2.
- Плитка — сама контейнер (`container: rdcard`), вид считается от её размера:
  уже 100 px — только значок и дата «дд.мм» (от 36 px высоты);
  шире — значок и подпись в 1–2 строки; от 56 px высоты — вторая строка с датой
  прошлой выгрузки («ещё не выгружали», если файла нет).
- Крупная плитка (от 110×120 px): значок крупно сверху (кегль от плитки), подпись до 3 строк
  по центру, дата внизу — большой блок не пустует даже с одной выгрузкой.

### Матрица (образец)
```
report_download — Выгрузка отчёта (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    Tf    f     f     f     f     f     ·     ·     
4    Tf    f     f     ·     ·     ·     ·     ·     
6    T     ·     ·     ·     ·     ·     ·     ·     
8    T     ·     ·     ·     ·     ·     ·     ·     
10   T     ·     ·     ·     ·     ·     ·     ·     
12   T     ·     ·     ·     ·     ·     ·     ·     
14   T     ·     ·     ·     ·     ·     ·     ·     
16   T     ·     ·     ·     ·     ·     ·     ·     
18   T     ·     ·     ·     ·     ·     ·     ·     
20   T     ·     ·     ·     ·     ·     ·     ·     
22   T     ·     ·     ·     ·     ·     ·     ·     
24   T     ·     ·     ·     ·     ·     ·     ·     
26   T     ·     ·     ·     ·     ·     ·     ·     
28   T     ·     ·     ·     ·     ·     ·     ·     
30   T     ·     ·     ·     ·     ·     ·     ·     
32   T     ·     ·     ·     ·     ·     ·     ·     
Итого: T многоточие: 16; f подгон спрятал строки: 9
```
Живые данные (одна выгрузка, файлов нет): 2×2 — T (заголовок) и e, остальное чисто; на 16×16
одна крупная плитка. Без заголовка (2x2, 4x2, 8x4): 2×2 — e (значок и дата в крошечном блоке),
остальное чисто. `render_widgets.php report_download` — 14 проверок, 0 ошибок.

### Аудит 23.09.2026
- Все 7 выгрузок белого списка проверены через ядро (пользователь 1, `mode=excel`): ответ 200,
  `Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`,
  `Content-Disposition: attachment; filename=…xlsx`, тело — ZIP (`PK`) 8–94 КБ. Файлы,
  которые создала проверка, удалены из `storage/app/public/temp`.
- Неточная ссылка: у «Договоров и оплат» заголовок никуда не вёл (у страницы нет
  именованного маршрута). Теперь он ведёт на `/report/payments`: пятым элементом `REPORTS`
  можно указать путь.
- Комментарий в классе поправлен: файлы лежат в `temp/` диска по умолчанию
  (`storage/app/public/temp`), а не в `storage/app/temp`.
- Подсказки настроек. «Первый — главная кнопка…» не совпадало с вёрсткой (все плитки
  равны), теперь «Плитки идут в порядке списка…». У даты добавлено: имя файла у
  «Конфигураций» и «Договоров и оплат» одно, поэтому и дата общая.
- Живые данные (2x2 … 16x32): чисто, на 2×2 — T (заголовок) и e.

### Что осталось
- T в колонке ширины 2 — заголовок «Выгрузка отчёта» в общей шапке.
- e на 2×2 без заголовка/с живыми данными — крошечный блок, значок кнопки виден.
- У `.btn` Metronic отступы заданы селектором с шестью `:not()`: в стилях плитки пришлось
  `padding … !important`.

## Популярные сценарии — `scenarios_top`

Правки: вьюха переписана, стили в `analytics.css` (плитки итогов `an-tiles` — общие с `china`
и `specs_summary`), `sample()` даёт до 30 сценариев с учётом «Сколько сценариев» и
«Показывать лицензии», как `data()`. `data()` не менялся.

### Поведение по размерам
- ширина 2: итоги столбиком — всего упоминаний, сценариев, лицензий; в высоком блоке вразрядку.
- ширина ≥4, высота 2: список «место — название — упоминания», доля ≥230 px; строки до
  `$rows_max` в `.desk-fit`, подпись «и ещё N».
- высота 3–5: над списком строка итога — упоминаний в КП, · сценариев (≥230 px),
  · период (≥420 px).
- высота ≥6: плитки итогов — упоминаний, сценариев, лицензий (≥230 px) и подпись
  «в КП за всё время».
- ширина ≥12 при высоте ≥3: таблица # / сценарий / доля / всего; ≥620 px + группа и лицензии.
- высокий блок, где все сценарии влезают с запасом (`count × 2,5 ≤ $rows`): подробные строки
  на любой ширине — название и значок, полоска относительно лидера с долей, группа и лицензии
  (≥230 px). Лишнее прячет `.desk-fit`.

### Матрица (образец, «Сколько сценариев» = 10)
```
scenarios_top — Популярные сценарии (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    Tf    Tf    Tf    Tf    f     f     f     f     
4    T     Tf    Tf    Tf    f     f     f     f     
6    T     Tf    Tf    Tf    f     f     f     f     
8    T     Tf    Tf    Tf    f     f     f     f     
10   T     T     T     T     ·     ·     T     T     
12   T     T     T     T     ·     ·     T     T     
14   T     T     T     T     ·     ·     T     T     
16   T     T     T     T     ·     ·     T     T     
18   T     T     T     T     ·     ·     T     T     
20   T     T     T     T     ·     ·     ·     ·     
22   T     T     T     T     ·     ·     ·     ·     
24   T     T     T     T     ·     ·     ·     ·     
26   T     T     T     T     ·     ·     ·     ·     
28   T     T     T     T     ·     ·     ·     ·     
30   T     T     T     T     ·     ·     ·     ·     
32   T     T     T     T     ·     ·     ·     ·     
Итого: T многоточие: 74; f подгон спрятал строки: 29
```
Живые данные (2x2, 4x2, 8x8, 16x16, 16x32): только T и f. Без заголовка (2x2, 4x2, 8x4): T и f,
на 2×2 влезают всего и сценариев. «Сколько сценариев» = 30 (4x8, 8x16, 8x32, 16x16, 16x32):
T и f. `render_widgets.php scenarios_top` — 16 проверок, 0 ошибок.

### Аудит 23.09.2026
- **Неточность: счёт по всем редакциям КП.** Запрос брал основной вариант каждой строки
  `proposals`, то есть каждой редакции, и КП в пяти редакциях давал пять упоминаний.
  Было 1292 упоминания, 77 сценариев и 252 472 лицензии; первым шло «Распознавание марки и
  модели машин» (115). Теперь считается только последняя редакция (`MAX(id)` по `group`, как
  scope `latestIteration` и отчёт): 587 упоминаний, 75 сценариев, 122 552 лицензии, первое место
  у «Присутствие / отсутствие человека в зоне» (54). Это до строки совпадает с рейтингом
  `/report/popular_scenario` (`ReportService::scenario_popular`), включая места.
  Второстепенные КП связки не считаются (`ProposalLink::notSecondarySql`, как `counted()`).
- Места: равное число упоминаний даёт одно место (1, 2, 2, 4…), как в рейтинге отчёта.
- «По спецификациям»: отменённые спецификации больше не считаются (было 15 строк, стало 12),
  как в «Конфигурациях, сводной».
- Подписи периода: «текущий квартал» → «за текущий квартал» (под плитками «в КП за текущий
  квартал»). Пустое состояние: «Сценариев в КП за текущий квартал нет». Даты периода — в
  `title`. Образец учитывает настройки источника и отбора; подписи вариантов настройки
  уточнены.
- Живые данные (2x2 … 16x32): только T на названиях сценариев и группах, иначе f.

### Что осталось
- T в колонке ширины 2 — заголовок «Популярные сценарии» в общей шапке.
- T на ширине 4–8 — названия сценариев (`.desk-grow` с `title`); на ширине 14–16 и высоте
  10–18 — группа в таблице («Промышленность», «Контроль транспортных средств», ячейка до 9rem
  с `title`). Числа, доли и лицензии целиком.
- При малом «Сколько сценариев» (3–5) огромный блок (16×32) заполнен меньше: строк в данных
  столько, сколько задал пользователь.
- Решает владелец: заголовок ведёт на «Список сценариев» (`/report/scenarios`) — это
  справочник без цифр. Рейтинг, с которым сходятся цифры (`/report/popular_scenario`),
  в меню выключен («Рейтинг сценариев (OLD)»).

## Конфигурации, сводная — `specs_summary`

Правки: вьюха переписана, стили в `analytics.css`, `sample()` даёт 40 партнёров (итоги
считаются по строкам). `data()` не менялся.

### Поведение по размерам
- ширина 2: итоги столбиком — спецификаций, конфигураций, строк состава, договоров, клиентов,
  партнёров, мимо справочника; сколько влезло — решает `.desk-fit`, в высоком блоке вразрядку.
- ширина ≥4: плитки в ряд — спецификаций; ≥230 px + конфигураций, ≥420 px + строк состава,
  ≥620 px + договоров.
- блок выше 110 px: строка пометок — партнёров, клиентов (≥230 px), мимо справочника и
  отбор (≥420 px).
- высота ≥6 со «Списком партнёров»: список партнёров до `$rows_max` в `.desk-fit` —
  название, полоска относительно лидера (≥230 px), число спецификаций; «и ещё N».
- высота ≥6 без списка: все шесть итогов плитками `desk-value` по три в ряд, вразрядку по
  высоте (лишние прячет `.desk-fit`), под ними мимо справочника и отбор.

### Матрица (образец)
```
specs_summary — Конфигурации, сводная (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    Tf    ·     ·     ·     ·     ·     ·     ·     
4    Tf    T     ·     ·     ·     ·     ·     ·     
6    T     Tf    f     f     f     f     f     f     
8    T     Tf    f     f     f     f     f     f     
10   T     Tf    f     f     f     f     f     f     
12   T     Tf    f     f     f     f     f     f     
14   T     Tf    f     f     f     f     f     f     
16   T     Tf    f     f     f     f     f     f     
18   T     Tf    f     f     f     f     f     f     
20   T     Tf    f     f     f     f     f     f     
22   T     Tf    f     f     f     f     f     f     
24   T     Tf    f     f     f     f     f     f     
26   T     Tf    f     f     f     f     f     f     
28   T     T     ·     ·     ·     ·     ·     ·     
30   T     T     ·     ·     ·     ·     ·     ·     
32   T     T     ·     ·     ·     ·     ·     ·     
Итого: T многоточие: 31; f подгон спрятал строки: 79
```
Живые данные (2x2, 4x2, 8x8, 16x16, 16x32): T и f, на 16×32 — e (в отчёте 5 партнёров,
список короткий). Без заголовка (2x2, 4x2, 8x4): T только на подписях 2×2. Без списка
партнёров (4x6 … 16x32): T на 4×6 и 4×16 (заголовок), остальное чисто.
`render_widgets.php specs_summary` — 16 проверок, 0 ошибок.

### Аудит 23.09.2026
- Сверка с `/report/specs` на живых данных (тот же `ReportSpecService::specs`):
  отфильтрованные — 6 партнёров, 7 клиентов, 7 договоров, 10 спецификаций, 3 конфигурации,
  3 строки состава; «все спецификации» — 7 / 9 / 9 / 13 / 3 / 10 и 1 мимо справочника.
  Независимый подсчёт по ключам `system` в ячейках отчёта даёт то же, а сумма списка
  партнёров равна числу спецификаций. Неточностей нет.
- В образце подпись отбора теперь зависит от настройки.
- Живые данные (2x2 … 16x32): T на заголовке и названии партнёра, e на 16×32 (в отчёте
  6 партнёров).

### Что осталось
- T в колонке ширины 2 — заголовок «Конфигурации, сводная» и подписи «спецификаций»,
  «конфигураций», «строк состава» (длинное слово шире 62 px; подписи с `title`, числа целиком).
- T на ширине 4 — заголовок в общей шапке.
- e на 16×32 с живыми данными — партнёров в отчёте 5, заполнять огромный блок нечем.

## Сценарии по спецификациям — `scenarios_specs`

Правки: вьюха переписана, стили в `analytics.css`, `sample()` даёт 40 строк отчёта или
20 сценариев (с учётом «Сколько строк» и «Разбивать по нейросервисам»). `data()` не менялся.

### Поведение по размерам
- ширина 2: итоги столбиком — клиентов, сценариев, спецификаций, партнёров, строк отчёта;
  сколько влезло — `.desk-fit`, в высоком блоке вразрядку.
- ширина ≥4: список до `$rows_max` в `.desk-fit` с «и ещё N». Разрез «Строки отчёта» —
  сценарий, клиент (≥230 px, до 45% строки); разрез «Сценарии» — сценарий, спецификаций
  (≥230 px), значок клиентов.
- высота 3–5: над списком строка итога — клиентов, · спецификаций (≥230 px), · сценариев
  (≥420 px), · всего строк (≥620 px).
- высота ≥6: плитки итогов — клиентов, сценариев, спецификаций (≥230 px), партнёров (≥420 px),
  строк отчёта (≥620 px).
- ширина ≥12 при высоте ≥3: таблица отчёта — клиент / спецификация / сценарий, ≥620 px +
  партнёр и нейросервис; в разрезе «Сценарии» — # / сценарий / спецификаций / клиентов,
  ≥620 px + доля клиентов полоской. В блоке высотой 2 вместо таблицы список.

### Матрица (образец, разрез «Строки отчёта»)
```
scenarios_specs — Сценарии по спецификациям (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    Tf    Tf    Tf    f     f     f     f     f     
4    Tf    Tf    Tf    Tf    f     Tf    Tf    Tf    
6    T     Tf    Tf    Tf    f     Tf    Tf    Tf    
8    T     Tf    Tf    Tf    f     Tf    Tf    Tf    
10   T     Tf    Tf    Tf    f     Tf    Tf    Tf    
12   T     Tf    Tf    Tf    f     Tf    Tf    Tf    
14   T     Tf    Tf    Tf    f     Tf    Tf    Tf    
16   T     T     T     T     ·     T     T     T     
18   T     T     T     T     ·     T     T     T     
20   T     T     T     T     ·     T     T     T     
22   T     T     T     T     ·     T     T     T     
24   T     T     T     T     ·     T     T     T     
26   T     T     T     T     ·     T     T     T     
28   T     T     T     T     ·     T     T     T     
30   T     T     T     T     ·     T     T     T     
32   T     T     T     T     ·     T     T     T     
Итого: T многоточие: 108; f подгон спрятал строки: 51
```
Разрез «Сценарии» (все 128 размеров): только T и f. «Разбивать по нейросервисам» (12x8 … 16x32):
T и f. Живые данные (2x2, 4x2, 8x8, 16x16, 16x32): T и f. Без заголовка (2x2, 4x2, 8x4): T и f.
`render_widgets.php scenarios_specs` — 16 проверок, 0 ошибок.

### Аудит 23.09.2026
- Сверка с `/report/scenarios_specs` на живых данных (`ReportService::getScenariosSpecsSummary`)
  совпала с развёрнутыми склейками отчёта: 15 строк (по нейросервисам), 2 партнёра,
  2 клиента, 2 спецификации, 7 сценариев.
- «Строк отчёта» в разрезе «Сценарии» считалось 15 (нейросервисы не схлопнуты), а в разрезе
  «Строки отчёта» — 8 (схлопнуты). Теперь число одно в обоих разрезах и зависит от галочки
  «Разбивать по нейросервисам».
- Вёрстка. В таблице отчёта у каждой `.desk-cut` ширина 99%, и первая колонка забирала
  почти всю ширину: «Спецификация 1» и «ССР» уходили в многоточие даже на 16 колонках.
  Теперь у колонок заданы доли: клиент 22 %, спецификация 25 %; от 620 px — партнёр 15 %,
  клиент 20 %, спецификация 20 %, нейросервис 15 %. Сценарий берёт остаток.
  Клиент в строке списка больше не сжимается (`flex: 0 0 auto`, не шире 45 %).
- Коды в матрице те же, но многоточий стало меньше: «Спецификация N» и названия клиентов
  больше не режутся. В многоточии остаются длинные названия сценариев и партнёров (с `title`).
- Живые данные (2x2 … 16x32, 32x8): T на названиях сценариев и партнёров, e на 16×32
  (в отчёте 8 строк).

### Что осталось
- T в колонке ширины 2 — заголовок «Сценарии по спецификациям» и подписи «спецификаций»,
  «строк отчёта» (с `title`, числа целиком).
- T на ширине 4–8 — названия сценариев в строках списка; на ширине 12–16 — длинные сценарии
  и партнёры в таблице отчёта (`.desk-cut` с `title`; после аудита 23.09 спецификации и клиенты
  не режутся). Строк отчёта «Сколько строк» (по умолчанию 20), поэтому на высоте ≥16 подгону
  прятать нечего.

# Прогон по сетке: группа funnel-1

Виджеты: `country_month`, `country_quarter`, `crm_mismatch`, `deal_card`, `deal_issues`,
`deals_quarter`, `deals_registry`. Стили группы — `public/metronic/css/osmo-desktop-widgets/funnel-1.css`.

Общее по всем виджетам группы: **T в шапке** («Страны × статусы помесячно» и т.п.) на ширине 2–4 —
это заголовок оболочки (`partials/widget.blade.php`); виджет его не рисует и не правит. С 23.09 у
`.desk-widget-title` есть `title` — многоточие в шапке допустимо.

## `country_month` — Страны × статусы помесячно

Файлы: вьюха `country_month.blade.php` (переписана), `CountryMonthWidget::sample()` (8 стран ×
5 статусов ≈ 40 строк вместо 8), стили в `funnel-1.css`.

### Поведение по размерам

- ширина 2–5 (`$dw` xs|sm): общая сумма крупно (`.desk-value`, без символа — он в подписи);
  высота ≥3: + подпись «₽ за 6 мес.» и страны по убыванию суммы, ниже «По месяцам» — суммы
  месяцев (`.desk-list.desk-fit`, месяцы уходят первыми, «ещё N»); на ширине 2 название
  и сумма в две строки, сумма не режется;
- ширина 6–11 (`md`): матрица «страна × месяцы» — статусы свёрнуты в страну; первая колонка
  месяца всегда, следующие появляются по ширине блока (~64 px на месяц, контейнерные запросы),
  колонка «Итого» (за весь период) и итоговая строка — всегда;
- ширина ≥12 (`lg|xl`): матрица «страна → статус × месяцы» (при «Строки: страна → статус»),
  страна узкой колонкой, статус — широкой; при 6 месяцах на ширине 16 видны все;
- высота: строки таблицы — `$rows_max` в `.desk-fit` (`tbody > tr`, `data-fit-min=0`), итог в
  `tfoot` остаётся; на высоте 2 — шапка месяцев и итоговая строка;
- высокий блок (`$dh` lg|xl), а строк не больше половины `$rows` (живые данные: 3–4 страны) —
  под таблицей столбчатый график сумм по месяцам на всё свободное место.

### Финальная матрица (образец)

```
country_month — Страны × статусы помесячно (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     T     f     f     f     f     f     f     
4    Tf    Tf    f     f     f     f     f     f     
6    Tf    Tf    f     f     f     Tf    Tf    Tf    
8    Tf    Tf    ·     ·     ·     Tf    Tf    Tf    
10   Tf    Tf    ·     ·     ·     Tf    Tf    Tf    
12   Tf    T     ·     ·     ·     Tf    Tf    Tf    
14   Tf    T     ·     ·     ·     Tf    Tf    Tf    
16   Tf    T     ·     ·     ·     Tf    Tf    Tf    
18   T     T     ·     ·     ·     Tf    Tf    Tf    
20   T     T     ·     ·     ·     Tf    Tf    Tf    
22   T     T     ·     ·     ·     Tf    Tf    Tf    
24   T     T     ·     ·     ·     Tf    Tf    Tf    
26   T     T     ·     ·     ·     T     T     T     
28   T     T     ·     ·     ·     T     T     T     
30   T     T     ·     ·     ·     T     T     T     
32   T     T     ·     ·     ·     T     T     T     
Итого: T многоточие: 74; f подгон спрятал строки: 56
```

Живые данные (2x2, 4x2, 4x16, 8x8, 12x12, 16x16, 16x32): только T (шапка) и f; на 8x8 и 16x16
график 338×178 и 716×528. Без заголовка (2x2, 4x2, 8x2, 8x4, 16x2): 2x2 — `e`, остальное f/·.
12 месяцев (4x32, 6x4 … 16x8, 16x2): T только на статусах, f.

### Аудит 23.09.2026

- **Сверка с воронкой** (таблица «Страны и статусы помесячно», `DashboardDataService::country_status_month()` —
  тот же метод): суммы по месяцам и странам совпадают до рубля в ₽ и € (сен 19,9 млн, окт 14,3, ноя 23,9,
  дек 119,4, янв —, фев 80,0; итого 257,5 млн ₽; China / Russia / Spain). Месяцы — от текущего (сен 2026).
- **Подпись подгона** «ещё {n} строк в таблице» давала «ещё 1 строк» — теперь «ещё строк: {n}».
- Многоточие — только шапка оболочки, названия стран на ширине 2 и длинные стадии («Execution
  (PRE-PAYMENT)») в колонке статуса, всё с `title`; суммы и итоги не режутся.

### Что осталось

- **T** на ширине 2–4 — заголовок оболочки; на ширине 2 ещё названия стран в списке (с `title`);
- **T** на ширине 12–16 — длинные названия стадий («Execution (PRE-PAYMENT)») в колонке статуса,
  с `title`; числа и суммы без многоточия во всех размерах;
- **f** — строки спрятаны подгоном (или спрятанная подпись «ещё N» при полном списке) — норма;
- **e** — только 2x2 без заголовка (одно число).

## `country_quarter` — Страны × статусы поквартально

Файлы: вьюха `country_quarter.blade.php` (переписана по образцу `country_month`),
`CountryQuarterWidget::sample()` (8 стран × 5 статусов), стили — общие с `country_month`
правила в `funnel-1.css` (selector list на оба id).

### Поведение по размерам

- ширина 2 (`$dw` xs): общая сумма `.desk-value-sm` (иначе «1,4 млрд» уходит в многоточие);
  ширина 3–5: `.desk-value`; высота ≥3: + «₽ за 4 кв.», страны по убыванию суммы, затем
  «По кварталам» (`.desk-fit`, кварталы уходят первыми, «ещё N»);
- ширина 6–11: матрица «страна × кварталы» (статусы свёрнуты), кварталы по ширине блока;
- ширина ≥12: матрица «страна → статус × кварталы»; на ширине 16 видны все 4 квартала;
- предупреждение «Вне таблицы: N сделок на S» (части — в подсказке) — под таблицей/списком на всех ширинах, переносится
  по словам; на узком блоке (≤170 px) сумма прячется (`desk-hide-narrow`, полностью в `title`),
  на низком (≤96 px) — всё предупреждение;
- высота: строки в `.desk-fit`, итог в `tfoot`; высокий блок с малым числом строк — график по
  кварталам под таблицей.

### Финальная матрица (образец)

```
country_quarter — Страны × статусы поквартально (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     T     f     f     f     f     f     f     
4    Tf    Tf    f     f     f     f     f     f     
6    Tf    Tf    f     f     f     f     f     f     
8    Tf    Tf    ·     ·     ·     Tf    Tf    f     
10   Tf    Tf    ·     ·     ·     Tf    Tf    f     
12   Tf    T     ·     ·     ·     Tf    Tf    f     
14   Tf    T     ·     ·     ·     Tf    Tf    f     
16   Tf    T     ·     ·     ·     Tf    Tf    f     
18   T     T     ·     ·     ·     Tf    Tf    f     
20   T     T     ·     ·     ·     Tf    Tf    f     
22   T     T     ·     ·     ·     Tf    Tf    f     
24   T     T     ·     ·     ·     Tf    Tf    f     
26   T     T     ·     ·     ·     Tf    Tf    f     
28   T     T     ·     ·     ·     T     T     ·     
30   T     T     ·     ·     ·     T     T     ·     
32   T     T     ·     ·     ·     T     T     ·     
Итого: T многоточие: 58; f подгон спрятал строки: 59
```

Живые данные (2x2, 4x2, 2x16, 4x16, 8x8, 12x12, 16x16, 16x32): T (шапка, «Saudi Arabia» на
ширине 2, статус на 12x12) и f; графики 338×168 (8x8), 716×398 (16x16). Без заголовка: 2x2 — `e`,
остальное f/·. 8 кварталов (4x32, 6x4 … 16x8, 16x2): T только на статусах, f.

### Аудит 23.09.2026

- **Сверка с воронкой** («Страны и статусы поквартально», III'26–II'27 — как `quarterColumns()` после правки
  22.09): по кварталам и странам совпадает до рубля в ₽ и $ (итого 554,6 млн ₽).
- **Неточность — предупреждение «Не выбрано: 13 сделок на 378 млн ₽»**: ни у одной из 13 сделок квартал
  не был «не выбран». 6 сделок — прошедшие кварталы (2026q1–q2, сделки всё ещё в воронке, 49,3 млн),
  7 — IV'27, позже последнего столбца (328,2 млн). Теперь `data()` делит их на части (`none_parts`:
  `empty` / `past` / `later`), подпись — «Вне таблицы: N сделок на S» («Без квартала», если у всех квартал
  не выбран), части с суммами — в подсказке. `sample()` отдаёт те же ключи.
- «ещё {n} строк в таблице» → «ещё строк: {n}».
- Живые (2x2, 4x8, 8x8, 16x8, 16x16): T только шапка оболочки, f.

### Что осталось

- **T** на ширине 2–4 — заголовок оболочки и названия стран на ширине 2 (с `title`);
- **T** на ширине 12–14 — «Execution (PRE-PAYMENT)» и подобные стадии (с `title`);
- **e** — только 2x2 без заголовка.

После правки `country_month` тоже получил `.desk-value-sm` на ширине 2 — матрица прежняя.

## `crm_mismatch` — Расхождения с Битрикс24

Файлы: вьюха `crm_mismatch.blade.php` (переписана), `CrmMismatchWidget::sample()` (10 разных КП,
до 30 строк, режется настройкой `limit` — как `data()`), `fields()`: у «Сколько КП в списке»
значение по умолчанию 30 вместо 8 (сколько строк показать, решает высота блока; подсказка
«Не больше, чем влезает по высоте блока»), стили в `funnel-1.css`.

### Поведение по размерам

- всегда — число КП с расхождениями (`.desk-value`, красное);
- ширина 2: подпись прячется (`desk-hide-narrow`); высота ≥3 — «на +335 тыс.» («на» переносится,
  сумма нет), ниже номера КП (`2026-120`) с подсказкой — название, компания, причины;
- высота ≤5 и ширина ≥3: сводка одной строкой — число (`desk-value-sm` на высоте 3–5), подпись,
  сумма расхождения и значки видов (на высоте 2: сумма от 230 px, значки от 420 px);
- высота 3–5: + список КП в `.desk-fit` — название, сумма расхождения (шире 170 px), вид (от 420 px);
- высота ≥6: число крупно, в том же `.desk-fit` сначала виды расхождений со счётчиками (подписи
  шире 170 px), затем КП — подгон прячет КП первыми;
- ширина ≥12 и есть список: сетка — слева сводка и виды (30 %), справа таблица КП на всю высоту
  (`.desk-fit`, `tbody > tr`): КП, «Что не так», расхождение; статус от 620 px, компания от 860 px.

### Финальная матрица (образец)

```
crm_mismatch — Расхождения с Битрикс24 (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     T     ·     ·     ·     ·     ·     ·     
4    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
6    Tf    Tf    f     f     f     Tf    Tf    Tf    
8    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
10   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
12   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
14   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
16   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
18   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
20   Tf    Tf    Tf    f     f     T     T     T     
22   Tf    Tf    Tf    f     f     T     T     T     
24   T     Tf    Tf    f     f     T     T     T     
26   T     T     T     ·     ·     T     T     T     
28   T     T     T     ·     ·     T     T     T     
30   T     T     T     ·     ·     T     T     T     
32   T     T     T     ·     ·     T     T     T     
Итого: T многоточие: 91; f подгон спрятал строки: 78
```

Живые данные (2x2, 4x2, 2x8, 4x4, 8x4, 8x8, 12x6, 16x16, 16x32): только T и f, 16x32 занято 61 %.
Без заголовка (2x2, 4x2, 6x2, 8x4, 16x2, 16x4): 2x2 — `e` (одно число), остальное ·/f/T.

### Аудит 23.09.2026

- **Сверка со страницей** `/crm-monitor`: 61 КП с расхождениями, по видам 57 / 2 / 0 / 3 / 2 / 1 — совпадает
  (тот же `CrmMismatchService::rows()`; второстепенные КП связки исключены там же `counted()`). Ссылки
  строк — сводная карточка `deal_card.index` по группе КП, как на странице.
- **Неточность**: сумма расхождения («на −310 млн») показывалась и при отборе другого вида (например,
  «Сделка не привязана»), хотя относится только к «Сумма не сходится»; страница показывает её только при
  отборе `amount`. Теперь — при «Любое» и «Сумма не сходится».
- **Вёрстка**: в таблице (ширина ≥12) колонка «Что не так» резалась даже на 16x16 («Сумма не сходится»,
  «Сделка не привязана»): у двух `.desk-cut` по 99 %, сумма больше 100, и браузер отдаёт место первой колонке.
  Доли заданы явно в `funnel-1.css`: КП 50 / «Что не так» 50, с колонкой компании (от 860 px) 38 / 24 / 38.
- Образец: номера КП в формате портала («OD620»), а не «2026-120».

### Что осталось

- **T** — шапка оболочки (ширина 2–4), названия КП в списке и таблице, «Что не так» и подписи
  видов в левой колонке на ширине 12–16 — всё с `title`; числа и суммы без многоточия;
- **e** при явно заданном `limit` = 8 на 16x32 — список короче блока по воле пользователя;
  по умолчанию (30) высокий блок заполнен.

## `deal_card` — Карточка сделки

Файлы: вьюха `deal_card.blade.php` (переписана), стили в `funnel-1.css`; класс не менялся
(у сделки фиксированный набор полей, заполнять высокий блок нечем, кроме лестницы стадий).

### Поведение по размерам

- низкий блок (высота ≤2): название (`title` — название и стадия) и сумма `.desk-value-sm`;
  на ширине 2 — сумма без символа, от 420 px — значок стадии;
- высота ≥3: шапка — название, «↗» в Битрикс24 (шире 170 px), на ширине ≥6 значок стадии;
  плитки «сумма, ₽» (число без символа — никогда не режется) и на ширине ≥6 «квартал плана»
  («IV кв. 2026»); кегль плиток — `clamp(13px, min(6.5cqw, 16cqh), 40px)`;
- поля строками в `.desk-fit`: стадия и квартал (ширина 2–5), партнёр, заказчик, ответственный,
  КП, проект; в блоке уже 230 px подпись над значением;
- высота ≥6 (`$dh` lg|xl), стадия из воронки: + «стадии воронки» — пройденные с галочкой, текущая
  жирным, будущие приглушены; подгон прячет их первыми; на ширине ≥12 — отдельной колонкой справа.

### Финальная матрица (образец)

```
deal_card — Карточка сделки (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     ·     ·     ·     ·     ·     ·     ·     
4    Tf    Tf    f     f     f     f     f     f     
6    Tf    Tf    f     f     f     f     f     f     
8    Tf    Tf    f     f     f     f     f     f     
10   Tf    Tf    f     f     f     ·     ·     ·     
12   Tf    Tf    f     f     f     ·     ·     ·     
14   Tf    Tf    ·     ·     ·     ·     ·     ·     
16   Tf    Tf    ·     ·     ·     ·     ·     ·     
18   Tf    T     ·     ·     ·     ·     ·     ·     
20   T     T     ·     ·     ·     ·     ·     ·     
22   T     T     ·     ·     ·     ·     ·     ·     
24   T     T     ·     ·     ·     ·     ·     ·     
26   T     T     ·     ·     ·     ·     ·     ·     
28   T     T     ·     ·     ·     ·     ·     ·     
30   T     T     ·     ·     ·     ·     ·     ·     
32   T     T     ·     ·     ·     ·     ·     ·     
Итого: T многоточие: 31; f подгон спрятал строки: 39
```

Живые данные — сделка 931 (с КП и проектом: 2x2, 4x2, 4x8, 8x4, 8x8, 16x16, 16x32) и 763
(4x4, 8x8, 16x16): только T и f. Без заголовка (2x2, 4x2, 8x2, 8x4, 16x2): T на 2x2, f.
Без выбранной сделки — «Сделка не выбрана» (`.desk-empty`).

### Аудит 23.09.2026

- **Живые данные**: сделка 931 — «Пилот. Прессовый цех», TCP, 184 тыс. ₽, IV кв. 2026, КП № AA787, ссылка
  на сводную карточку; пересчёт суммы сверен с воронкой в $ (сделки 195 в ₽ и 873 в €) — до цента.
  Проектов по сделкам в локальной базе нет (`deal_project_deals` пуст) — строка «проект: нет».
- **Неточность**: выбранная, но удалённая из Битрикс24 сделка показывалась как «Сделка не выбрана» —
  теперь «Сделки #N нет в Битрикс24» (значок `fa-link-slash`).
- Образец: КП «№ AA794 · …», как отдаёт `data()` (было «КП № AA-794» — под подписью «КП» слово дублировалось).

### Что осталось

- **T** на ширине 2–4 — название сделки, подписи и значения полей (партнёр, КП, проект…) —
  всё с `title`; сумма и квартал без многоточия;
- 16x32 занято 27 % — у одной сделки больше нечего показать; растягивать плитки не стал.

## `deal_issues` — Проблемы в сделках

Файлы: вьюха `deal_issues.blade.php` (переписана по схеме `crm_mismatch`), `DealIssuesWidget::sample()`
(40 сделок, у первых шести по две проблемы), сетка «сводка слева — таблица справа» — общие с
`crm_mismatch` правила в `funnel-1.css` (selector list).

### Поведение по размерам

- высота ≤2: подпись «проблемные сделки» (шире 170 px) и число по центру;
- высота 3–5: «4 сделки с проблемами» одной строкой (`desk-value-sm`; на ширине 2 — только число),
  ниже сделки в `.desk-fit`: значок (шире 170 px), название с `title` (стадия, менеджер, все
  проблемы), число замечаний значком;
- высота ≥6: число крупно; в том же `.desk-fit` сначала типы проблем со счётчиками, затем сделки —
  подгон прячет сделки первыми;
- ширина ≥12 и есть сделки: сетка — слева сводка и типы (30 %), справа таблица на всю высоту
  (`tbody > tr`): сделка, первая проблема, «+N» ещё, менеджер от 620 px. Итоговая строка таблицы
  убрана — число уже в сводке слева.

### Финальная матрица (образец)

```
deal_issues — Проблемы в сделках (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     ·     ·     ·     ·     ·     ·     ·     
4    Tf    Tf    f     f     f     Tf    Tf    Tf    
6    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
8    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
10   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
12   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
14   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
16   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
18   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
20   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
22   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
24   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
26   Tf    Tf    Tf    f     f     T     T     T     
28   Tf    Tf    Tf    f     f     T     T     T     
30   T     T     Tf    f     f     T     T     T     
32   T     T     T     ·     ·     T     T     T     
Итого: T многоточие: 90; f подгон спрятал строки: 101
```

Живые данные (2x2, 4x2, 2x8, 4x4, 8x4, 8x8, 12x6, 16x16, 16x32): T и f, на 16x32 — `e` (17 %):
в живой базе проблемных сделок меньше, чем строк в блоке, — пустота от данных, не от вьюхи.
Без заголовка (2x2, 4x2, 6x2, 8x4, 16x2, 16x4): 2x2 — `e` (одно число), остальное ·/f/T.

### Аудит 23.09.2026

- **Сверка**: 7 проблемных сделок — как в попапе «Проблемные сделки» воронки (`getDealWithIssues()`),
  счётчики 3 / 2 / 2; ссылки — сделки в Битрикс24.
- **Вёрстка**: колонка «Проблема» в таблице (ширина ≥12) резалась так же, как «Что не так» у
  `crm_mismatch` («Не указан партнёр» на 16x16) — доли 50 / 50 в `funnel-1.css`.
- Подпись «Не указана стоимость лиценцзий и(или) услуг» — опечатка в перечислении `CrmDealIssues`
  (модуль Bitrix), не правил: см. «Нужны общие правки».

### Что осталось

- **T** — шапка оболочки, названия сделок и формулировки проблем («Не заполнен месяц сделки или
  квартал») — с `title`; число сделок и счётчики без многоточия;
- **e** — 2x2 без заголовка и 16x32 на живых данных (мало сделок).

## `deals_quarter` — Сделки по кварталам

Файлы: вьюха `deals_quarter.blade.php` (переписана), стили в `funnel-1.css`; класс не менялся
(кварталов всегда четыре — образец заполняет блок графиком и таблицей).

### Поведение по размерам

- высота ≤2: ячейки в ряд — «план 2026, ₽» и сумма за год (`desk-value-sm`), «к 2025» и дельта,
  суммы I–IV кв.; не влезшие по ширине прячет `.desk-fit` с `data-fit-axis="x"` (на ширине 4
  только сумма, на 8 — сумма, дельта и 2 квартала, с 10 — всё); на ширине 2 — только сумма;
- ширина 2, высота ≥3: сумма за год, кварталы строками в `.desk-fit` (квартал, сумма, прошлый
  год столбиком); высота ≥6 — под ними столбики без осей (`sparkline`) на оставшееся место;
- ширина ≥3, высота ≥3: строка «2026 · 123 сделки» (число сделок — шире 230 px), дельта (шире 230 px), прошлый год
  (шире 420 px), сумма; ниже столбчатый график 2026/2025 на всё место; «без квартала: N на S» —
  переносится по словам, сумма прячется в узком блоке;
- высота ≥10 и ширина ≥6: под графиком таблица кварталов — сумма, прошлый год, дельта, число
  сделок (от 420 px) и итог в `tfoot`.

### Финальная матрица (образец)

```
deals_quarter — Сделки по кварталам (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     f     f     f     ·     ·     ·     ·     
4    Tf    ·     ·     ·     ·     ·     ·     ·     
6    Tf    ·     ·     ·     ·     ·     ·     ·     
8    Tf    ·     ·     ·     ·     ·     ·     ·     
10   Tf    ·     ·     ·     ·     ·     ·     ·     
12   T     ·     ·     ·     ·     ·     ·     ·     
14   T     ·     ·     ·     ·     ·     ·     ·     
16   T     ·     ·     ·     ·     ·     ·     ·     
18   T     ·     ·     ·     ·     ·     ·     ·     
20   T     ·     ·     ·     ·     ·     ·     ·     
22   T     ·     ·     ·     ·     ·     ·     ·     
24   T     ·     ·     ·     ·     ·     ·     ·     
26   T     ·     ·     ·     ·     ·     ·     ·     
28   T     ·     ·     ·     ·     ·     ·     ·     
30   T     ·     ·     ·     ·     ·     ·     ·     
32   T     ·     ·     ·     ·     ·     ·     ·     
Итого: T многоточие: 16; f подгон спрятал строки: 7
```

Живые данные (2x2, 4x2, 8x2, 2x16, 8x8, 16x16, 16x32): T и f; на 16x32 график 716×1241 и таблица.
Без заголовка (2x2, 4x2, 8x2, 8x4, 16x2): только · и f.

### Аудит 23.09.2026

- **Неточность — отменённые сделки в плане.** Отбор по умолчанию «Все стадии» считал и Canceled (семантика
  стадии F): в 2026-м 118 млн из 1 026, в 2025-м 308 млн из 686 — дельта «к 2025» считалась от раздутой
  базы. Теперь отбор «Все, кроме отменённых» (и «Только завершённые» — тоже без них): 2026 — 907 млн,
  2025 — 378 млн ₽. Сохранённые настройки (`all`) остаются рабочими.
- **«без квартала: 21 на 0 ₽»** в отборе «Стадии воронки»: сделки без суммы (лиды, ранние стадии) больше не
  идут в предупреждение, как и в `country_quarter`: «все» — 2 сделки на 5,7 млн, воронка — нет.
- **Вёрстка**: в блоке уже 230 px строка «2026 · 123 сделки» уходила в многоточие вместе с числом — число
  сделок теперь от 230 px (T 31 → 16).
- Сверка в отборе «Стадии воронки»: III кв. 170,6 млн и IV кв. 157,7 млн ₽ = столбцы III'26 / IV'26 таблицы
  воронки.

### Что осталось

- **T** на ширине 2 — шапка оболочки; суммы, дельты, число сделок и кварталы без многоточия;
- график на ширине 2 и высоте ≥6 — узкие столбики без осей, но выше 40 px (не G).

## `deals_registry` — Реестр сделок

Файлы: вьюха `deals_registry.blade.php` (переписана), `DealsRegistryWidget::sample()` (40 сделок
из 5 образцов, режется настройкой `limit` — как `data()`), `fields()`: у «Сколько сделок» значение
по умолчанию 50 вместо 12 (сколько строк показать, решает высота блока), стили в `funnel-1.css`.

### Поведение по размерам

- высота ≤2: ячейки в ряд — число сделок по отбору (`desk-value-sm`, подпись — вкладка), «с КП»,
  «без КП», последняя сделка с датой; не влезшие по ширине прячет `.desk-fit` по оси x; на ширине 2
  — только число («сделок»);
- высота ≥3: строка сводки — вкладка (шире 170 px), число, «с КП» (шире 230 px), значок «отбор»
  (шире 420 px); ниже сделки в `.desk-fit` (`$rows_max`, не больше `limit`), «ещё N»;
- ширина 2–11 — список: точка стадии (шире 170 px), название с `title` (партнёр, заказчик, стадия,
  дата, менеджер, сумма), номер КП (от 420 px), сумма; на ширине 2 название и сумма в две строки,
  сумма без символа валюты;
- ширина ≥12 — таблица (`tbody > tr`): сделка, стадия (точка + текст вместо значка, который резался
  в узкой ячейке), дата, сумма, КП; от 620 px ещё ID и менеджер, от 800 px — партнёр.

### Финальная матрица (образец)

```
deals_registry — Реестр сделок (образцовые данные, ячейка 47.25 px)
h\w  2     4     6     8     10    12    14    16    
2    T     f     f     ·     ·     ·     ·     ·     
4    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
6    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
8    Tf    Tf    Tf    f     f     Tf    Tf    Tf    
10   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
12   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
14   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
16   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
18   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
20   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
22   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
24   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
26   Tf    Tf    Tf    f     f     Tf    Tf    Tf    
28   Tf    T     T     ·     ·     T     T     T     
30   Tf    T     T     ·     ·     T     T     T     
32   Tf    T     T     ·     ·     T     T     T     
Итого: T многоточие: 91; f подгон спрятал строки: 101
```

Живые данные (2x2, 4x2, 12x2, 2x8, 4x4, 8x8, 12x6, 16x16, 16x32): только T и f (до правки на 2x8
была C — «184 тыс. ₽» шире блока, теперь сумма без символа). Без заголовка (2x2, 4x2, 8x2, 8x4,
16x2): 2x2 — `e` (одно число), остальное ·/f.

### Аудит 23.09.2026

- **Сверка**: 299 сделок по «Все сделки, КП любое», с КП 108 — как страница `/bitrix/deal?has_proposal=all`
  (тот же `CrmDealRegistryService::rows()`); `sourceUrl()` ведёт туда же.
- **Неточность**: символ валюты брался из своей таблицы ₽ $ € ¥ — у сделок в рупиях (5 в реестре) было
  «INR». Теперь из справочника валют портала (`DesktopContext::symbol()`: ₹; у сума символа нет — «UZS»).
- **Вёрстка**: в таблице колонки «Стадия» и «Менеджер» резались («Contracting» на 12x6, «Анна Смирнова» на
  16x8) — та же беда с двумя `.desk-cut` по 99 %. Доли: сделка 60 / стадия 40; от 620 px 46 / 30 / 24
  (менеджер); от 800 px 38 / 24 / 20 / 18 (партнёр). Длинные стадии на 12–16 по-прежнему с `title`.
- Образец: номера КП «AA795», название КП — как у сделки.

### Что осталось

- **T** — шапка оболочки (ширина 2), названия сделок в списке и таблице, стадии и менеджеры в
  таблице на ширине 12–16 — всё с `title`; суммы, даты и число сделок без многоточия;
- на высоте ≥28 образец (40 сделок) помещается целиком — f пропадает, блок занят на 81 %.

## Итог по группе

| Виджет | X | C | H | F | G | T | e | s |
|---|---|---|---|---|---|---|---|---|
| `country_month` | 0 | 0 | 0 | 0 | 0 | 74 | 0 | 0 |
| `country_quarter` | 0 | 0 | 0 | 0 | 0 | 58 | 0 | 0 |
| `crm_mismatch` | 0 | 0 | 0 | 0 | 0 | 91 | 0 | 0 |
| `deal_card` | 0 | 0 | 0 | 0 | 0 | 31 | 0 | 0 |
| `deal_issues` | 0 | 0 | 0 | 0 | 0 | 90 | 0 | 0 |
| `deals_quarter` | 0 | 0 | 0 | 0 | 0 | 16 | 0 | 0 |
| `deals_registry` | 0 | 0 | 0 | 0 | 0 | 91 | 0 | 0 |

Прогон 23.09.2026 (после аудита). T везде — шапка оболочки на ширине 2–4 и названия (стран, стадий, КП,
сделок, проблем) с `title`; суммы, числа и счётчики без многоточия. Число T по размерам у таблиц не
уменьшилось (в размере остаются длинные названия), но внутри таблиц `crm_mismatch`, `deal_issues`,
`deals_registry` перестали резаться короткие подписи («Сумма не сходится», «Contracting», менеджер).
`render_widgets.php` — 104 проверки без ошибок. `php -l` — чисто по всем изменённым классам.

### Нужны общие правки (не делал)

1. ~~`partials/widget.blade.php`: у `.desk-widget-title` нет `title`~~ — сделано (на 23.09 `title` есть);
   T в шапке на ширине 2–4 теперь с подсказкой.
2. Настройки «Сколько КП/сделок в списке» (`crm_mismatch`, `deals_registry`) теперь по умолчанию
   максимальные. На уже сохранённых столах остались старые 8/12 — там высокие блоки короче;
   поправить можно миграцией настроек, если владелец захочет.
3. Узкий список «название над суммой» (`flex-wrap` + `flex-basis: 100%` в контейнере уже 130 px)
   повторён в трёх виджетах группы — кандидат в общий класс вроде `.desk-list-stack-narrow`.
4. `CrmDealIssues::LICENCE_OR_SERVICE_AMOUNT_DOESNT_FILL` (модуль Bitrix): в подписи опечатка
   «лиценцзий» — видна в `deal_issues` и в попапе воронки.
5. `deals_quarter`, отбор «Все, кроме отменённых» (по умолчанию): больше половины суммы — стадия
   Suspended (2026: 521 млн из 907; 2025: 332 млн из 378). Считать ли приостановленные сделки планом —
   решение владельца; можно исключить их так же, как отменённые, или сделать отбором по умолчанию
   «Стадии воронки».

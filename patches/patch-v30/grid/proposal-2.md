# Прогон по сетке — группа proposal-2

Виджеты: `proposal_status`, `proposals_aging`, `proposals_by_manager`, `proposals_mine`,
`proposals_nodeal`, `proposals_recent`. Стили группы —
`public/metronic/css/osmo-desktop-widgets/proposal-2.css`.

Общее для всех: **T в колонке ширины 2** — это многоточие в заголовке виджета
(`.desk-widget-title` в `partials/widget.blade.php`), тело виджета там без многоточия.
`title` у заголовка уже есть (общая правка сделана), стенд всё равно считает многоточие.

## Аудит 23.09.2026 — связка КП, сверка с источником

**Связка КП (patch v33).** Все шесть виджетов берут строки из
`ProposalStatusService::latestIterations()` — там `Proposal::counted()`; отбор по периоду —
`MetricRegistry::groupsSentBetween()`, тоже с `counted()`. Второстепенные КП в счётчики статусов,
возраст, разрез по менеджерам и «без сделки» не попадают (локально: 204 последних редакции,
в расчётах 203 — без AK760; «В работе» 104, не 105). Списки «Последние КП» и «Мои КП» теперь
показывают второстепенные так же, как список `/proposals` (коммит f3143a6): веткой сразу под
строкой главного — уголок, приглушённая строка, в таблице вместо статуса плашка «второстепенное»,
в списке от 420 px та же плашка; смены статуса нет, в «всего», «за неделю», «старше N дн.» и в
лимит «Сколько КП» ветки не входят (`ProposalsRecentWidget::packTree()`, ветки одним запросом).
Проверено на паре ВДНХ (менеджер — пользователь 5): «Мои КП» по сумме — AA793, под ним AK760;
«Последние КП» (только мои, 50) — то же. В «КП без сделки» веток нет намеренно: второстепенным
становится только КП без сделок, привязать сделку к нему сервер не даёт.

**Сверка со списком `/proposals`** (`ProposalRepository::getTable()`, та же выборка `counted()`):
всего 203 = 203; по статусам 104 / 29 / 70 = виджет «КП по статусам»; по вкладкам менеджеров
51 / 38 / 22 / 40 / 52 = отбор `manager_id` в виджетах; без сделки 91, в работе 86 = фильтр
«Сделка: нет» списка. Последние редакции по `MAX(id)` (виджеты) и `MAX(iteration)` (список)
совпадают полностью.

**Неточности, исправлены:**
- «Последние КП»: «за неделю: N» считался только по показанным строкам (лимит 10) — живьём
  было 10, на деле 81. Теперь `data()['fresh']` по всем КП отбора.
- «Мои КП»: «старше 14 дн.: K» — то же, по показанным строкам: было 10, на деле 17 из 22.
  Теперь `data()['stale_count']`.
- «Мои КП», сортировка «По сумме»: сравнивались числа в разных валютах (1,5 млн ¥ ниже 2 млн ₽).
  Теперь порядок — по сумме в рублях по курсу на сегодня (`CurrencyService::convertAmount()`),
  показывается по-прежнему в валюте КП: AA663 1 500 400 ¥ (≈ 18,8 млн ₽) встал между 18,8 и
  17,9 млн ₽, AA765 96 328 $ — к 8 млн ₽.
- «Последние / Мои / без сделки»: нулевая сумма основного варианта показывалась «0 ₽», в списке
  КП — прочерк (AA595). Теперь «—» и «Нет расчёта» в подсказке.
- «Возраст КП»: крупное «в работе» не считало КП без даты отправки (они вне корзин), при всех
  КП без даты виджет писал «КП в работе нет». Теперь `in_work` = корзины + без даты (сейчас
  таких 0, число 104 = «КП по статусам»).
- «КП по менеджерам»: пустое состояние всегда «КП за период нет» — и при «Все КП», и при
  отборе по статусу; подпись показателя («КП, шт.», «выиграно, шт.») была только при отборе
  по периоду. Теперь подпись всегда: период или «все КП», статус, показатель; пустое —
  «КП со статусом «…» за период нет» по настройкам. Живьём в низком широком блоке (14–16×2)
  при двух менеджерах строка пустовала (e) — справа итог «всего КП» от 420 px.
- «КП по статусам»: в низком блоке отбор по периоду и «только мои» нигде не назван — теперь
  в подсказках бейджей, конверсии и числа.
- «КП без сделки»: настройка «Только мои» не отражалась в подписи; пустое состояние «Все КП
  привязаны к сделкам» и при «только в работе». Теперь «без сделки, в работе, мои» и «Все мои
  КП в работе привязаны к сделкам».
- Значок «КП по менеджерам» `fa-users-line` (FA 6.1+) заменён на `fa-users` ещё до аудита;
  остальные значки группы в FA 6.0 есть.

## КП по статусам — `proposal_status`

Поведение по размерам:
- высота 2, ширина 2: только число «В работе» (цветная точка), остальные счётчики и конверсия — в `title`;
- высота 2, ширина ≥4: строка бейджей «значок + число» по трём статусам; уже 230 px значки
  прячутся; от 320 px справа конверсия; от 580 px в бейджах подписи статусов;
- высота ≥4: четыре плитки (три статуса + конверсия), каждая — контейнер `pstile`, число
  считается от размера плитки. Раскладка по пропорции блока: шире 2:1 — в ряд, уже 7:10 —
  в колонку, иначе 2×2;
- плитка ниже 44 px — точка и число в строку; выше 82 px — под числом доля статуса
  («34,3 %», у конверсии «из решённых»); плитка уже 70 px — без подписи, только точка;
- блок выше 230 px — внизу шкала долей `.desk-split`;
- ширина 2 — конверсия без десятых («73 %»), иначе не влезает.

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    Te    ·     ·     ·     ·     ·     ·     ·
4    T     ·     ·     ·     ·     ·     ·     ·
6    T     ·     ·     ·     ·     ·     ·     ·
8    T     ·     ·     ·     ·     ·     ·     ·
10   T     ·     ·     ·     ·     ·     ·     ·
12   T     ·     ·     ·     ·     ·     ·     ·
14   T     ·     ·     ·     ·     ·     ·     ·
16   T     ·     ·     ·     ·     ·     ·     ·
18   T     ·     ·     ·     ·     ·     ·     ·
20   T     ·     ·     ·     ·     ·     ·     ·
22   T     ·     ·     ·     ·     ·     ·     ·
24   T     ·     ·     ·     ·     ·     ·     ·
26   T     ·     ·     ·     ·     ·     ·     ·
28   T     ·     ·     ·     ·     ·     ·     ·
30   T     ·     ·     ·     ·     ·     ·     ·
32   T     ·     ·     ·     ·     ·     ·     ·
Итого: T многоточие: 16; e занято < 1/4: 1
```

Живые данные (сетка целиком, 23.09): T 16 (заголовок в ширине 2), e 1 (2x2); 104 / 29 / 70,
конверсия 29,3 % — как список КП. Правка 23.09 — только подсказки, матрица прежняя.
Без заголовка (2x2, 4x2, 8x4): e 1 (2x2). `render_widgets.php` — 14 проверок, ошибок 0.

Осталось: T — заголовок виджета в блоках шириной 2; e на 2x2 — одно число в крошечном блоке,
допустимо.

## Возраст КП — `proposals_aging`

Поведение по размерам (раскладку выбирает PHP по `$dw`/`$dh`, данные — 4–5 корзин):
- высота 2: число «в работе» слева, справа чипы корзин от старых к свежим, не влезшие прячет
  подгон по ширине (`data-fit-axis="x"`); в ширине 2 чипов не остаётся — только число;
- ширина 2, высота ≥4: число и столбик чисел корзин с цветной точкой, самая старая сверху;
- высота 3–5, ширина 3–11: число со сводкой (старейшая корзина; «самое старое» от 230 px,
  «в среднем» и «без даты» от 420 px), шкала долей и чипы корзин (подгон по ширине);
  кегль числа от `24cqh`, иначе в широком блоке чипы уходили за нижний край;
- высота 3–5, ширина ≥12: число со сводкой слева (не шире 45 %), справа плитки корзин —
  подпись, число, полоска, доля;
- высота ≥6, ширина 3–5: число, шкала и список корзин в `.desk-fit`; в блоке выше 380 px
  строки расходятся по высоте;
- высота ≥6, ширина ≥6: число со сводкой и столбики корзин (высота — от самой полной корзины,
  подпись «30–60», доля под столбиком — в блоке выше 300 px).

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    Tf    f     f     f     ·     ·     ·     ·
4    Tf    f     f     f     ·     ·     ·     ·
6    T     ·     ·     ·     ·     ·     ·     ·
8    T     ·     ·     ·     ·     ·     ·     ·
10   T     ·     ·     ·     ·     ·     ·     ·
12   T     ·     ·     ·     ·     ·     ·     ·
14   T     ·     ·     ·     ·     ·     ·     ·
16   T     ·     ·     ·     ·     ·     ·     ·
18   T     ·     ·     ·     ·     ·     ·     ·
20   T     ·     ·     ·     ·     ·     ·     ·
22   T     ·     ·     ·     ·     ·     ·     ·
24   T     ·     ·     ·     ·     ·     ·     ·
26   T     ·     ·     ·     ·     ·     ·     ·
28   T     ·     ·     ·     ·     ·     ·     ·
30   T     ·     ·     ·     ·     ·     ·     ·
32   T     ·     ·     ·     ·     ·     ·     ·
Итого: T многоточие: 16; f подгон спрятал строки: 8
```

Живые данные (сетка целиком, 23.09): T 16, f 8 — как образец; «в работе» 104, из них 90+ дн. — 87.
После правки (2x2, 4x2, 8x4, 8x8, 16x8, 16x16, 16x32): T 1, f 3.
`render_widgets.php` — 14 проверок, ошибок 0.

Осталось: T — заголовок в ширине 2 (на 2x2 ещё подпись «в работе», у неё `title`);
e на 2x2 без заголовка — одно число; f — чипы и строки корзин, спрятанные подгоном.

## КП по менеджерам — `proposals_by_manager`

Поведение по размерам:
- высота 2, ширина 2: лидер — имя (многоточие, `title`) и число;
- высота 2, ширина ≥4: лидеры в строку «имя число» — 1 (ширина 3–5), до 3 (6–11), до 8
  (≥12); не влезшие по ширине прячет подгон; от 420 px справа итог «всего КП» (23.09);
- высота ≥4, ширина ≤11: подпись отбора — период или «все КП», статус, показатель (выше 110 px;
  слово «отправленные» от 230 px; до 23.09 подписи не было без отбора по периоду),
  список менеджеров до `$rows_max` в `.desk-fit` и «ещё N»; полоска от 230 px, «+выиграно»
  от 420 px; итог: «всего КП» (слово от 170 px), «выиграно»/сумма от 230 px, «менеджеров»
  от 340 px;
- ширина ≥12: таблица — Менеджер, КП/Сумма; Доля от 420 px; В работе / Выиграно / Проиграно
  от 620 px; строки таблицы подгоном (`data-fit-items="tbody > tr"`);
- образец `sample()` — 40 менеджеров (было 5).

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    T     ·     f     f     f     f     f     f
4    Tf    f     f     f     f     f     f     f
6    Tf    f     f     f     f     f     f     f
8    Tf    f     f     f     f     f     f     f
10   Tf    f     f     f     f     f     f     f
12   Tf    f     f     f     f     f     f     f
14   Tf    f     f     f     f     f     f     f
16   Tf    f     f     f     f     f     f     f
18   Tf    f     f     f     f     f     f     f
20   Tf    f     f     f     f     f     f     f
22   Tf    f     f     f     f     f     f     f
24   Tf    f     f     f     f     f     f     f
26   Tf    f     f     f     f     f     f     f
28   T     ·     ·     ·     ·     ·     ·     ·
30   T     ·     ·     ·     ·     ·     ·     ·
32   T     ·     ·     ·     ·     ·     ·     ·
Итого: T многоточие: 16; f подгон спрятал строки: 102
```

Живые данные (сетка целиком до правки, 23.09): T 31 (заголовок и имена в ширине 2–4, с `title`),
e 2 (14x2, 16x2 — два менеджера за квартал), f 1. После правки (вся высота 2, 8x4, 16x8) e нет:
справа «всего КП: 20».
`render_widgets.php` — 16 проверок, ошибок 0.

Осталось: T — заголовок и имена менеджеров в ширине 2 (имена с `title`); f — строки,
спрятанные подгоном. На 16x32 f даёт сама подпись «ещё N», когда прятать нечего:
`osmo-desktop-fit.js` вешает на неё `desk-fit-out`, стенд считает это спрятанной строкой.

## Последние КП — `proposals_recent`

Поведение по размерам:
- ширина 2: точка статуса и номер КП, компания, статус и сумма — в `title` строки;
- ширина 3–11, а также любая ширина при высоте 2: список — точка статуса, номер (от 170 px),
  компания (многоточие, `title`), значок «изменено за неделю» от 230 px, дата изменения
  от 420 px, сумма; строки до `$rows_max` в `.desk-fit` и «ещё N»;
- ширина ≥12 и высота ≥3: таблица — Номер, Компания, Сумма, Статус; Название и Изменено
  от 620 px; строки подгоном (`data-fit-items="tbody > tr"`); в высоту 2 таблица не ставится —
  шапка и строка не влезают;
- выше 96 px — итог внизу: «всего КП» (слово от 170 px), «за неделю: N» и ссылка «все КП»
  от 230 px;
- сколько КП, решает настройка «Сколько КП» в `data()`, вьюха по ней больше не режет;
  образец `ProposalsRecentWidget::sampleRows()` — 40 строк (был 5; общий с «Мои КП» и
  «КП без сделки»);
- второстепенные КП связки (23.09) — ветка под своим главным: в списке уголок на месте точки
  статуса, строка приглушена, плашка «второстепенное» от 420 px; в таблице уголок перед номером
  и плашка вместо статуса; `title` — «Второстепенное к № …: только просмотр». Образец —
  одна ветка под второй строкой (`sampleBranch()`).

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    Tf    Tf    f     f     f     f     f     f
4    Tf    Tf    f     f     f     f     Tf    Tf
6    Tf    Tf    f     f     f     f     Tf    Tf
8    Tf    Tf    f     f     f     f     Tf    Tf
10   Tf    Tf    f     f     f     f     Tf    Tf
12   Tf    Tf    f     f     f     f     Tf    Tf
14   Tf    Tf    f     f     f     f     Tf    Tf
16   Tf    Tf    f     f     f     f     Tf    Tf
18   Tf    Tf    f     f     f     f     Tf    Tf
20   Tf    Tf    f     f     f     f     Tf    Tf
22   Tf    Tf    f     f     f     f     Tf    Tf
24   Tf    Tf    f     f     f     f     Tf    Tf
26   Tf    Tf    f     f     f     f     Tf    Tf
28   T     T     ·     ·     ·     ·     T     T
30   T     T     ·     ·     ·     ·     T     T
32   T     T     ·     ·     ·     ·     T     T
Итого: T многоточие: 62; f подгон спрятал строки: 104
```

Живые данные (23.09): T и f только на названиях компаний и подгоне; «всего КП: 203»,
«за неделю: 81» (до правки 10). Пользователь 5, «только мои», 50: AA793 и ветка AK760.
`render_widgets.php` — 16 проверок, ошибок 0.

Осталось: T в ширине 2 — заголовок; в ширине 4 — названия компаний в строках; в ширине
14–16 — колонка «Название» таблицы (`desk-cut`). У всех `title`. Живой блок при настройке
«Сколько КП» = 10 показывает 10 строк и итог внизу, остальное место пустое: это выбор
пользователя.

## Мои КП — `proposals_mine`

Поведение по размерам:
- ширина 2: значок статуса (попап смены статуса) и номер КП, жёлтый у старых; компания,
  давность и сумма — в `title` строки;
- ширина 3–11, а также любая ширина при высоте 2: список — значок статуса, номер (от 170 px),
  компания (многоточие, `title`), давность от 230 px (жёлтая у старых), сумма; строки до
  `$rows_max` в `.desk-fit` и «ещё N»;
- ширина ≥12 и высота ≥3: таблица — Номер, Компания, Сумма, Давность, Статус (попап);
  Название от 620 px; строки подгоном;
- выше 96 px — итог внизу: «всего» (слово от 170 px), «старше N дн.: K» и ссылка «все КП»
  от 230 px;
- число КП ограничивает настройка «Сколько КП» в `data()`, образец — 40 строк;
- второстепенные КП связки (23.09) — ветка под своим главным, как в «Последних КП»: уголок на
  месте значка статуса (попапа смены статуса нет), без жёлтой подсветки давности; в таблице
  плашка «второстепенное» вместо статуса. «старше N дн.» — по всем КП отбора из `data()`.

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    f     Tf    f     f     f     f     f     f
4    f     Tf    f     f     f     f     Tf    Tf
6    f     Tf    Tf    f     f     f     Tf    Tf
8    f     Tf    Tf    f     f     f     Tf    Tf
10   f     Tf    Tf    f     f     f     Tf    Tf
12   f     Tf    Tf    f     f     f     Tf    Tf
14   f     Tf    Tf    f     f     f     Tf    Tf
16   f     Tf    Tf    f     f     f     Tf    Tf
18   f     Tf    Tf    f     f     f     Tf    Tf
20   f     Tf    Tf    f     f     f     Tf    Tf
22   f     Tf    Tf    f     f     f     Tf    Tf
24   f     Tf    Tf    f     f     f     Tf    Tf
26   f     Tf    Tf    f     f     f     Tf    Tf
28   ·     T     T     ·     ·     ·     T     T
30   ·     T     T     ·     ·     ·     T     T
32   ·     T     T     ·     ·     ·     T     T
Итого: T многоточие: 60; f подгон спрятал строки: 104
```

Живые данные (23.09): пользователь 1 — «всего: 22», «старше 14 дн.: 17» (до правки 10);
пользователь 5 по сумме, 50 — AA793 и ветка AK760. Первый вариант ветки давал C в ширине 2–4
(уголок и значок статуса не влезали) — уголок поставлен на место значка, C нет.
`render_widgets.php` — 14 проверок, ошибок 0.

Осталось: T — названия компаний в строках списка (ширина 4–6: рядом давность и сумма) и
колонка «Название» таблицы в ширине 14–16, у всех `title`. Короткий заголовок «Мои КП»
в ширине 2 не режется.

## КП без сделки — `proposals_nodeal`

Поведение по размерам:
- высота 2, ширина ≤5: счётчик с подписью «без сделки, в работе»;
- высота 2, ширина ≥6: счётчик слева (не шире трети), справа номера КП бейджами, не влезшие
  прячет подгон по ширине; в колонке сбоку подпись без «Битрикс24»;
- ширина 2, высота ≥4: счётчик, под ним столбик номеров КП со значком «привязать»;
- ширина 3–11, высота ≥4: счётчик (кегль от `20cqh`, чтобы под ним уместились строки),
  пояснение (от 170 px), список — номер, компания (многоточие, `title`), сумма от 230 px,
  значок «привязать» (попап `proposal.box_deal`); строки до `$rows_max` в `.desk-fit` и «ещё N»;
  в блоке выше 230 px внизу подсказка про значок и ссылка «все КП»;
- ширина ≥12, высота ≥4: счётчик слева, справа таблица — Номер, Компания, Сумма, «привязать»;
  Название и Менеджер от 620 px; строки подгоном, внизу подсказка и ссылка «все КП»
  (живой блок 16x32 с короткой таблицей больше не пустует снизу);
- сколько КП, решает настройка «Сколько КП в списке» в `data()`, вьюха по ней больше не режет;
  образец — 40 строк, счётчик образца — по их числу (было жёстко 12).

Финальная матрица (образец):

```
h\w  2     4     6     8     10    12    14    16
2    T     ·     Tf    ·     ·     ·     ·     ·
4    Tf    Tf    f     f     f     f     Tf    Tf
6    Tf    Tf    f     f     f     f     Tf    Tf
8    Tf    Tf    f     f     f     f     Tf    Tf
10   Tf    Tf    Tf    f     f     f     Tf    Tf
12   Tf    Tf    Tf    f     f     f     Tf    Tf
14   Tf    Tf    Tf    f     f     f     Tf    Tf
16   Tf    Tf    Tf    f     f     f     Tf    Tf
18   Tf    Tf    Tf    f     f     f     Tf    Tf
20   Tf    Tf    Tf    f     f     f     Tf    Tf
22   Tf    Tf    Tf    f     f     f     Tf    Tf
24   Tf    Tf    Tf    f     f     f     Tf    Tf
26   Tf    Tf    Tf    f     f     ·     T     T
28   T     T     T     ·     f     ·     T     T
30   T     T     T     ·     ·     ·     T     T
32   T     T     T     ·     ·     ·     T     T
Итого: T многоточие: 74; f подгон спрятал строки: 95
```

Живые данные (23.09, 2x4, 4x2, 8x4, 8x8, 16x8): T и f — подпись в ширине 2, компании, подгон;
86 без сделки в работе = фильтр списка. До аудита: T 3, f 3; без заголовка T 1 (2x2), f 1.
`render_widgets.php` — 12 проверок, ошибок 0.

Осталось: T в ширине 2 — заголовок и подпись «без сделки, в работе»; 6x2 — та же подпись
в колонке сбоку; ширина 4–6 — названия компаний в строках; ширина 14–16 — «Название» и
«Менеджер» таблицы. У всех `title`.

## Общие правки

Все три просьбы прошлого прогона сделаны (проверено 23.09): `.desk-only-*` прячут через
`display: none !important` в `@container` и больше не проигрывают `d-*`; у `.desk-widget-title`
есть `title`; стенд не считает подпись «ещё N» спрятанной строкой (`widget_grid.js`). По аудиту
23.09 новых правок в общих файлах не нужно.
